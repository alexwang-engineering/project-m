# Project M — Active Work Ledger

This file is the lock. Per `Project-M-Collaboration-Plan.md` §2: only the integration owner edits this file; chat messages and the desktop copy of the plan are not authoritative because they go stale. An agent may start a package only when it appears here with status `ACTIVE`, one named owner, one branch/worktree (where applicable), and declared owned paths.

State machine: `PLANNED → READY → ACTIVE → REVIEW → MERGED`, with `BLOCKED` reachable from any non-merged state.

## Known state as of ledger creation (2026-08-04)

- `main` is at `648f166` (Phase 1: schema, canonical router, dashboard).
- Branch `codex/phase1`, worktree `../project-m-codex`, is at `a920262` — **two separate Codex sessions wrote to this branch/worktree**: mine (MCP `codex` tool) produced `06dfd5e`; the user's own Codex desktop app produced `a920262` directly afterward, unregistered in any ledger. Landed serially, no conflict, but this is the exact failure mode Rule 1 exists to prevent. `a920262` renames `packPage`/`unpackPage` → `packageMpx`/`unpackMpx`, adds a PDF-only assertion, adds a combined-archive cap, and adds `package-lock.json`/`node_modules` (an `npm install` was run in that worktree).
- This divergence is **not yet reconciled**. It is queued as part of Package A (Baseline integration), which is itself blocked on Package 0. It must not be merged to `main` ad hoc before Package A runs.

## Ledger

| Package ID | Title | Owner | Branch | Worktree | Base commit | Owned paths | Status | Blockers | Handoff commit |
|---|---|---|---|---|---|---|---|---|---|
| Package 0 | Product boundary and irreversible decisions (PM-01..PM-05) | Codex integration; human product owner approves | `integration/package-0` | `../project-m-package-0` | `main`@`d6040f8` | `docs/coordination/**`, `docs/product/**`, `docs/adr/**` | MERGED | None; factual school/tenant inputs remain tracked as pre-staging gates | `b4d3dc7` |
| Package A | Baseline integration | Codex | `integration/baseline` | `../project-m-integration` | `main`@`275c26e` + reconcile `codex/phase1`@`a920262` | entire repo (serial); dispositions constrained by `PACKAGE-A-PREFLIGHT.md` | MERGED | None | `c6d5725` |
| Package B | Runnable application foundation | Codex | `codex/package-b-foundation` | `../project-m-foundation` | `main`@`c615a3c` | root config, dependency manifests, `lib/supabase/**`, env example, test config, minimum app shell; compilation-only updates to `app/[...slug]/page.tsx`; temporary `components/page-renderer.tsx` adapter | MERGED | None | `1403373` |
| Package C | App shell and design system | Claude | `claude/phase2-app-shell` | `../project-m-app-shell` | `main`@`7f5786d` | `app/layout.tsx`, `app/globals.css`, `app/page.tsx`, `components/ui/**`, `components/Dashboard.tsx` (presentation only) | MERGED | None | `7d19277`, merged at `de96258` |
| Package D | Database migration and policy hardening | Codex | `codex/package-d-database` | `../project-m-database` | `main` after Package B closeout | `supabase/migrations/**`, `supabase/tests/**`, generated DB types | MERGED | None — real Docker execution completed 2026-08-05 (see change log), found and fixed a base-GRANT bug; 66/66 pgTAP tests now genuinely passing | `811a241`, grant fix `4e0e7ab` |
| Package E | Content contracts and authorized read layer | Codex | `codex/package-e-content` | `../project-m-content` | `main` after Package D closeout | `lib/content/**`, content-contract tests, canonical resolver tests; compilation-only route wiring; one append-only content-contract migration, matching DB tests, regenerated DB types | MERGED | None | `a6644af` |
| Package F | Hardened MPX interchange | Codex | `codex/package-f-mpx` | `../project-m-mpx` | `main` after Package E closeout | `lib/mpx-packager.ts`, MPX contract/tests/docs only | MERGED | None; authoritative imports must repeat validation server-side | `2e8d571` |
| Package G | Authorized page mutation actions | Codex | `codex/package-g-mutations` | `../project-m-mpx` (reused after Package F merge) | `main` after Package F closeout | `lib/content/mutations.ts`, `app/actions/pages.ts`, mutation tests only | MERGED | None | `b911404` |
| Package H | Private file service contracts | Codex | `codex/package-h-files` | `../project-m-mpx` (reused; branch pointer assigned at handoff) | `main` after Package G closeout | `lib/files/**`, `app/actions/files.ts`, file-service tests; one append-only file-access RPC migration, matching DB tests, regenerated DB types | MERGED | Trusted malware/checksum completion worker and staging configuration remain separate deployment work | `3172bc0` |
| Package I | Tenant-restricted Entra authentication backend | Codex | `codex/package-i-auth` | `../project-m-mpx` (reused) | `main` after Package H closeout | `lib/auth/**`, `app/auth/**`, auth tests, protected-route proxy/env/config changes; one fail-closed Before User Created hook migration, matching DB tests, regenerated DB types | MERGED | Real tenant ID, app registration, redirect URIs, guest policy, and emergency owner are required before enabling staging | `a1a30f8` |
| Package J | Audited page revision restore | Codex | `codex/package-j-revisions` | `../project-m-mpx` (reused) | `main` after Package I closeout | `lib/content/revisions.ts`, `app/actions/revisions.ts`, revision tests; one append-only restore RPC migration, matching DB tests, regenerated types | MERGED | None | `aa81d96` |
| Package K | Audited administration API contracts | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`0888c95` | `lib/content/admin.ts`, `app/actions/admin.ts`, `app/admin/**`, `components/admin/**`, `supabase/tests/011_admin_operations.sql` | MERGED | Builds a UI/actions layer over the 4 existing admin RPCs from Package D (`20260804192000_audited_administration.sql`) — no new RPCs, no schema changes | `1448ad5` |
| Package L | Minimal assignment submission slice (MVP scope, ADR-008 addendum) | Claude | none (worked directly on `main`, single-directory repo post-relocation) | `~/Projects/project-m` | `main`@`b348446` | `supabase/migrations/20260805090000_*`, `supabase/migrations/20260805100000_*`, `supabase/tests/007_assignments.sql`, `docs/adr/008-*` addendum | MERGED | Submission UI shipped separately as Package M | `599b7ab` |
| Package M | Assignment submission UI | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`f00f3e7` | `app/assignments/**`, `app/actions/assignments.ts`, `components/assignments/**`, `lib/content/assignments.ts`, `lib/supabase/service.ts`, `lib/files/service.ts` (added `completeFileUpload` only), `app/actions/files.ts` (added `completeFileUploadAction` only), `lib/relative-time.ts` (extracted), `components/Dashboard.tsx` (nav link only) | MERGED | Teacher review UI shipped separately as Package N; live multi-step upload flow still not exercised end-to-end with seeded data/real auth session | `397931a` |
| Package N | Teacher submission review UI | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`cf7b8df` | `supabase/migrations/20260805120000_*`, `supabase/tests/008_teacher_submission_review.sql`, `app/assignments/[id]/**`, `components/assignments/SubmissionsView.tsx`, `lib/content/assignments.ts` (added `getAssignmentDetail` only) | MERGED | Live flow verification landed as Package O | `119af37` |
| Package O | Real end-to-end live verification (seeded data, real auth, full click-through) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`bfaddbe` | `supabase/migrations/20260806050000_*`, `components/Dashboard.tsx`, `components/assignments/AssignmentsView.tsx`, `components/assignments/SubmissionsView.tsx` (hydration fix only) | MERGED | `app/dev-login` is a deliberate, uncommitted local-only test scaffold (see change log) - must not be committed; real Entra SSO still unconnected so this remains the only way to get a real session locally | `6b392e1` |
| Package P | Pages editor UI, quizzes (new), assignment grading + gradebook | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`0356833` | `supabase/migrations/20260806060000_*`, `20260806080000_*`, `20260807010000_*`, `supabase/tests/009_*`, `010_*`, `app/pages/**`, `app/quizzes/**`, `app/gradebook/**`, `app/actions/quizzes.ts`, `app/actions/assignments.ts` (grading action only), `components/pages/**`, `components/quizzes/**`, `components/gradebook/**`, `lib/content/pages-editor.ts`, `lib/content/quizzes.ts`, `lib/content/gradebook.ts`, `lib/content/assignments.ts` (grading fns only), `lib/files/client-hash.ts`, `lib/files/service.ts` (image MIME types only), `components/page-renderer.tsx` (full rewrite), `lib/content/repository.ts` (tagIds field only) | MERGED | Package K (admin API contracts) and `page_editors` tag-bypass gap still open, unrelated to this package | `1e05a62` |
| Package K (follow-up) | Tag creation + assignment creation UI | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`189c820` | `supabase/migrations/20260807020000_create_tag.sql`, `supabase/tests/011_admin_operations.sql` (additive), `lib/content/admin.ts` (`createTag` only), `app/actions/admin.ts` (`createTagAction` only), `components/admin/CreateTagForm.tsx`, `app/admin/page.tsx` (renders the form only), `lib/content/assignments.ts` (`createAssignment` only), `app/actions/assignments.ts` (`createAssignmentAction` only), `components/assignments/AssignmentEditor.tsx`, `app/assignments/new/**`, `components/assignments/AssignmentsView.tsx` (header button only) | MERGED | None; `page_editors` tag-bypass gap still open, unrelated | `4910ba4` |
| Package Q | Calendar (deadline aggregation + standalone events) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`cb66f53` | `supabase/migrations/20260807030000_calendar.sql`, `supabase/tests/012_calendar.sql`, `lib/content/calendar.ts`, `app/actions/calendar.ts`, `app/calendar/**`, `components/calendar/**`, `lib/content/admin.ts` (extracted `isInstitutionAdmin` only), `app/admin/page.tsx` (import only), `components/Dashboard.tsx` (nav link only) | MERGED | Scope fixed by ADR-011 (one-off events only, whole-school broadcast flag `institution_admin`-only, simple upcoming-list UI — no recurrence, no grid view, no messaging/announcements) | `be36a68` |
| Package R | Announcements (one-way broadcast; NOT threaded messaging) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`cb6b907` | `supabase/migrations/20260807040000_announcements.sql`, `supabase/tests/013_announcements.sql`, `lib/content/announcements.ts`, `app/actions/announcements.ts`, `app/announcements/**`, `components/announcements/**`, `components/Dashboard.tsx` (nav link only) | MERGED | Scope fixed by ADR-012 (no read-state, no rich text/attachments, no rate limiting/moderation — posting is already staff/admin-only). **Threaded/bidirectional messaging is a separate, explicitly BLOCKED item, not part of this package** — pending a named human safeguarding owner per the collaboration plan's `C1-05`; no data model or code should be written for it until that owner is identified | `ec6aaf0` |
| Package S | Parent/guardian access (identity, admin-attested pupil links, read-only deadlines/grades/announcements) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`70d62a0` | `supabase/migrations/20260807050000_guardian_access.sql`, `supabase/tests/014_guardian_access.sql`, `supabase/tests/005_auth_admission.sql` (regression check, additive), `lib/content/guardians.ts`, `app/actions/guardians.ts`, `app/parent/**`, `app/auth/guardian-callback/**`, `components/parent/**`, `components/admin/AdminRoster.tsx` (guardian panel only), `app/admin/page.tsx` (wiring only) | MERGED | Scope fixed by ADR-013: real email magic-link sign-in (not Entra), admin-attested links (not MIS-verified), all three of deadlines/grades/announcements included, read access via new isolated `guardian_view_*` SECURITY DEFINER functions rather than modified RLS policies. **A DPIA and named privacy/safeguarding sign-off remain required before any real guardian data is processed in production** — same pre-launch-gate treatment as the Entra tenant (ADR-002), not resolved by this package | `072c6ae` |
| Package T | Question bank (tag-scoped, shared, copy-on-import into quizzes) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`ac34880` | `supabase/migrations/20260807060000_question_bank.sql`, `supabase/tests/015_question_bank.sql`, `lib/content/question-bank.ts` (new), `app/actions/question-bank.ts` (new), `app/question-bank/**` (new), `components/question-bank/**` (new), `lib/content/quizzes.ts` (`create_quiz` bank-item validation branch only), `components/quizzes/QuizEditor.tsx` (bank-import UI only), `components/quizzes/QuizzesView.tsx` (nav link only), `app/quizzes/new/page.tsx` (bank items fetch only), `lib/database.types.ts` (hand-patched, additive) | MERGED | Scope fixed by ADR-014 (tag-scoped/shared, single read/manage authorization tier, copy-on-import, `create_quiz` gains an optional `bankItemId` question shape rather than a new RPC). **Shipped on static verification only** — same Colima VM registry block that hit the gap-fixes above persisted through two more `colima start` attempts this session (confirmed via a direct `docker pull hello-world` retry, not just `supabase start`); pgTAP (`015_question_bank.sql`) and a live click-through (create a bank item, import it into a quiz, take the quiz, archive the item and confirm the already-created quiz is unaffected) remain unrun in any session | `e3e40b4` |
| Package U | MIS/SIS roster sync (file-based reconciliation engine; live connector deferred) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`c303390` | `supabase/migrations/20260807070000_mis_roster_sync.sql`, `supabase/tests/016_mis_roster_sync.sql`, `lib/content/roster-csv.ts` (new), `lib/content/roster-sync.ts` (new), `app/actions/roster-sync.ts` (new), `components/admin/RosterSyncPanel.tsx` (new), `app/admin/page.tsx` (panel wiring only), `lib/database.types.ts` (hand-patched, additive) | MERGED | Scope fixed by ADR-015: CSV-upload reconciliation only, no live MIS API/webhook (none exists in this environment — deferred per `PACKAGE-B-PREFLIGHT.md`'s own stated gate); role grants additive-only (never auto-revoked); tag memberships reconciled but only ones the sync itself granted (`tag_memberships.source = 'mis_sync:<run_id>'`) — any other-sourced membership is never touched; a leaver the sync previously provisioned is disabled (not deleted), re-enabling stays a separate deliberate admin action, never automatic; `institution_admin` can never be granted via this path. Also fixes `provision_admitted_institutional_user` (`on conflict (id) do nothing`) to prevent a duplicate-key race against the new `apply_mis_roster_intents` trigger once Entra is eventually enabled — found by design review, not live testing (same as the ADR-013 fix to this same function). **Shipped on static verification only, like the two packages above** — a fourth `colima start` attempt this session (explicitly requested) hit the identical registry-unreachable symptom; `tsc`/`eslint`/`next build` all clean, but `supabase/tests/016_mis_roster_sync.sql` (21 assertions) has never run against real Postgres and no live upload has been exercised. This is also the most complex SQL written this session (temp tables inside a `search_path=''` SECURITY DEFINER function, a partial-unique-index `ON CONFLICT`, cross-trigger race analysis) with no execution to catch mistakes — treat with more caution than earlier packages until verified | `4edce50` |
| Package V | Search (RLS-scoped full-text search, no new authorization logic) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`7b40c55` | `supabase/migrations/20260807080000_search.sql`, `supabase/tests/017_search.sql`, `lib/content/search.ts` (new), `app/actions/search.ts` (new), `components/search/SearchBox.tsx` (new), `lib/use-click-outside.ts` (new, extracted from `components/Dashboard.tsx`), `components/Dashboard.tsx` (search box wiring + hook extraction only), `docs/adr/016-search-scope.md` (new) | MERGED | Scope fixed by ADR-016: no new RPC and no new authorization logic at all — every searchable table (`pages`, `assignments`, `quizzes`, `announcements`, `calendar_events`) already has its own tested RLS select policy, so a plain `.textSearch()` call is authorized "inside source query" (PM-04's own stated requirement) by construction. Postgres native full-text search (`tsvector`/GIN index), not `ILIKE`, since PM-02 sets a real p95 <750ms target that a leading-wildcard `ILIKE` cannot meet at scale. Titles only for `pages` in v1 (its `content_json` is a structured block document, not plain text — flattening it is separate, named future work); `announcements`/`calendar_events` also index their existing plain-text body/description columns. Question bank items and guardian access are deliberately not searchable, matching their existing isolation boundaries (ADR-014/ADR-013) rather than needing new plumbing. **Verified further than the three packages above**: same static gate (`tsc`/`eslint`/`next build` all clean) plus an actual live browser check of the UI itself (search box renders, 1-character query correctly suppresses the dropdown, a 2+ character query round-trips through the real server action and renders "No results." cleanly with no console/network error) — genuine UI-layer verification, though still without a live Postgres to confirm real matches or the RLS-filtering property `supabase/tests/017_search.sql`'s 8 assertions exist to check | `1d63f10` |
| Package W | Operational reporting (audit log, roster/content summaries; compliance reporting deferred) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`2280205` | `lib/csv-export.ts` (new), `test/csv-export.test.ts` (new), `lib/content/reports.ts` (new), `app/actions/reports.ts` (new), `app/reports/**` (new), `components/reports/ReportsView.tsx` (new), `app/admin/page.tsx` (nav link only), `docs/adr/017-operational-reporting-scope.md` (new) | MERGED | Scope fixed by ADR-017, after asking the product owner directly given the plan's own Phase 5G acceptance gate states "compliance claims receive human approval" and X1-01 requires a "human reporting/privacy review" — same shape as the messaging/safeguarding split in ADR-012. Confirmed: build operational reporting only (audit log + roster/content summaries, admin-only, over data this project already has full RLS-backed authority for); real compliance exports (GDPR data-subject/erasure, safeguarding audit evidence) stay named and unbuilt pending a named human reviewer, generalizing ADR-013's existing DPIA gate rather than quietly satisfying it. **Zero new database migration or RPC** — every table read already has an `institution_admin`-bypass RLS policy (verified directly, not assumed), so this is the "no new authorization logic" principle from ADR-016 taken one step further. CSV export uses a real RFC4180 writer with OWASP formula-injection mitigation (`=`/`+`/`-`/`@`-prefixed cells get a leading apostrophe), since PM-04 and the plan's acceptance gate both name spreadsheet-formula injection explicitly. **Verified with real, executed tests for the first time all session on genuinely new logic**: `test/csv-export.test.ts` (7 vitest assertions, all passing, no Postgres needed) — one assertion initially failed for a real reason (a test payload containing both a formula-prefix character and a comma exercised the writer's *combined* quote+guard behavior, which was correct; the test's own isolation was wrong) and was fixed before being called done, not silently loosened. Static gate (`tsc`/`eslint`/`next build`) also clean, plus a live browser check confirming the `/reports` admin gate actually redirects a non-admin session away in a real running app, not just in code | `aa68423` |
| Package X | Accessibility (Launch v1: automated jsx-a11y/axe coverage + real fixes on reachable pages) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`c3cb347` | `eslint.config.mjs` (jsx-a11y rules only), `e2e/accessibility.spec.ts` (new), `app/auth/error/page.tsx` (new, replaces `route.ts`), `components/Dashboard.tsx` (skip link + main id + 2 contrast fixes), `components/search/SearchBox.tsx` (input aria-label + live region), ~30 `aria-label`/`htmlFor` additions across every create/edit form (`AssignmentEditor.tsx`, `QuizEditor.tsx`, `ReportsView.tsx`, `CreateEventForm.tsx`, `CreateTagForm.tsx`, `CreateAnnouncementForm.tsx`, `CreateBankItemForm.tsx`, `AdminRoster.tsx`, `SubmissionsView.tsx`, `BlockEditor.tsx`, `PageEditor.tsx`, `app/parent/login/page.tsx`), `docs/adr/018-accessibility-scope.md` (new) | MERGED | Scope fixed by ADR-018: automated regression coverage + real fixes on every page reachable without a Supabase session (Colima still blocked, 5th failed attempt this session), plus a source-review sweep across every authenticated form for the same static violation classes (unlike live-only surfaces, missing-label/missing-landmark defects are visible in JSX without needing real data behind them). Full live authenticated screen-reader walkthrough, exhaustive custom-hex contrast audit, and `prefers-reduced-motion` review stay named and deferred. **First package this session with real, executed, passing tests that found real bugs without needing Colima at all**: added `eslint-plugin-jsx-a11y` (found and fixed 6 real `label-has-associated-control` violations — a label sibling to its input, not wrapping or `htmlFor`-linked, so a screen reader announces nothing) and `@axe-core/playwright` (found and fixed 2 real WCAG 1.4.3 color-contrast failures on the dashboard, `text-slate-500`/bold `text-slate-400` against `bg-[#f7f8fa]`, re-verified green after). Also replaced `app/auth/error/route.ts` — a raw JSON API response explicitly marked "temporary" in its own comment — with a real accessible page (heading, readable message, working link) after live-testing it and finding literally a bare JSON dump, not a UI at all. Discovered but not fixed: every page renders its own `<header>` rather than sharing one layout component (14 near-identical copies), so the new skip link only covers the root page — recorded as an open item, since unifying it is a refactor, not an accessibility fix, and shouldn't be attempted unverified | `1ae0022` |
| Package Y | Moodle migration (staged import engine; real `.mbz` reader deferred) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`0faf3f5` | `supabase/migrations/20260807090000_content_migration.sql`, `supabase/tests/018_content_migration.sql`, `lib/content/migration.ts` (new), `lib/content/migration-types.ts` (new), `lib/content/migration-parse.ts` (new), `app/actions/migration.ts` (new), `components/admin/MigrationImportPanel.tsx` (new), `app/admin/page.tsx` (panel wiring only), `lib/files/client-hash.ts` (`sha256HexOfText` only, extracted alongside the existing file-hash helper), `test/migration-parse.test.ts` (new), `lib/database.types.ts` (hand-patched, additive), `docs/adr/019-moodle-migration-scope.md` (new) | MERGED | Scope fixed by ADR-019, same split as Package U (MIS/SIS sync): a staged import engine is fully buildable with no real Moodle instance, a real `.mbz` backup-format reader is not and stays deferred. Unlike Package U, almost no new SQL was needed — every actual content write reuses the already-tested `createPage`/`createAssignment`/`createQuiz` functions from Packages G/L/P rather than a new bulk path, so new SQL is limited to one tracking table plus two thin functions. Historical student submissions/grades are explicitly excluded, generalizing the same "named, deferred, pending human privacy review" gate already applied to guardian data (ADR-013) and compliance reporting (ADR-017) rather than treating a migration as exempt just because it's bulk. Idempotent and conflict-safe: an unchanged item is skipped, a changed item is reported as a conflict and never silently overwritten, tags are never auto-created, and everything lands in `draft` state for manual review. **Better-verified than Package U or T**: a real pure-logic unit surface (the manifest parser) got 6 genuine vitest assertions (51/51 total, no Postgres needed), on top of the usual `tsc`/`eslint`/`next build` gate, all passing. Still unverified: `supabase/tests/018_content_migration.sql` (10 assertions) has never run against real Postgres, and no live click-through of `/admin`'s new panel was possible — same admin-session constraint every admin-gated surface has had this session (Colima's 6th straight failed attempt) | `685b7bf` |
| Package Z | Security/production-readiness remediation (trusted file-verification worker; dev-login isolation; test repeatability; formatting gate; CSP headers) | Claude | none (worked directly on `main`) | `~/Projects/project-m` | `main`@`becee4e` | `supabase/migrations/20260808110000_*` (file-state worker schema), `lib/files/service.ts` (remove browser-callable completion), `lib/files/verification-worker.ts` (new), `lib/files/scanner.ts` (new), `scripts/verify-uploads.ts` (new), `app/actions/files.ts` (remove `completeFileUploadAction`, add read-only status action), `components/pages/BlockEditor.tsx` / `components/assignments/AssignmentsView.tsx` (poll for ready instead of self-completing), `lib/files/README.md` (rewrite), `test/file-verification.test.ts` (new), `proxy.ts` (CSP + dev-login production 404 guard — Next 16 nonce-based CSP requires proxy-level generation, not `next.config.ts`'s static `headers()`), `app/dev-login/page.tsx` / `app/dev-login/DevLoginClient.tsx` (server-component `NODE_ENV` guard, never committed), `supabase/migrations/20260808100000_profiles_read_quiz_takers.sql` (new; unrelated RLS gap found via live testing, fixed same session), `supabase/tests/001_content_authorization.sql` / `009_quizzes_and_grading.sql` / `012_calendar.sql` / `013_announcements.sql` (baseline-delta counting; `009` also gains the `profiles_read_quiz_takers` assertions), `lib/database.types.ts` (hand-patched: `scanning` enum value, `files.scanned_at`/`quarantine_reason`), `lib/supabase/service.ts` (deleted — zero remaining browser-reachable callers), `.github/workflows/ci.yml` (new: `app` / `database` / `secret-scan` jobs), `.gitignore` (dev-login coverage) | MERGED | External audit findings, delivered directly by the product owner mid-session with an explicit priority order (P0 file verification first, then dev-login isolation / test repeatability / full gate / CSP as P1). User's own clarification on P0: the fix must move file approval behind a **genuinely trusted worker boundary**, not a checksum bolted onto the existing browser-callable action. `app/dev-login/` stays untracked per instruction, never committed. Landed as 5 reviewable commits: `71a34b9` (formatting-only), `d33ac6f` (P0 worker), `56ae700` (dev-login isolation + CSP), `cbd7d82` (pgTAP repeatability + quiz-taker RLS gap), `609f022` (CI). Still genuinely blocked (not fabricated as resolved): real Entra tenant, production malware scanner, DPIA/guardian-data approval, named safeguarding/privacy/ops owners, monitoring/alerting, backup+PITR rehearsal, authenticated WCAG 2.2 AA walkthrough, representative-device performance testing. | `609f022`

## Change log

- 2026-08-04 — Ledger created. Package 0 marked ACTIVE (the only package the plan permits to start now). Divergence between Codex MCP session and Codex desktop session on `codex/phase1` recorded as a known blocker for Package A rather than silently reconciled.
- 2026-08-04 — PM-01 recorded: all eleven Moodle capabilities classified **Launch** (release-1, non-phased), confirmed after explicit reconsider. This is a major scope expansion beyond the plan's existing Phase 0–7 roadmap, which only covers the tagged-content-portal slice. **Package 0 remains ACTIVE, not complete** — PM-02 (user journeys, scale, targets), PM-03 (write authorization / canonical hierarchy / Entra tenant / lifecycle policy), PM-04 (threat model), and PM-05 (ADRs, plus a roadmap rewrite to add work packages for assignments, quizzes, gradebook, calendar, messaging, SCORM/LTI, parent access, MIS/SIS sync, migration, and reporting) are still outstanding.
- 2026-08-04 — PM-02 recorded: scale (medium, 800–2,500 users; staggered usage; ~500GB storage; mixed BYOD/variable WiFi), performance/availability/recovery/accessibility targets (2.5s LCP, school-hours support, hours-scale RTO/RPO tightening to <1hr before Phase 7, WCAG 2.2 AA), the top-5 journeys (student/teacher/admin/parent/cross-cutting), and the pilot success scorecard. **Package 0 remains ACTIVE** — PM-03, PM-04, PM-05 still outstanding.
- 2026-08-04 — **Product owner stepped away and instructed continued autonomous work without further stopping to ask.** From this point, decisions normally requiring human sign-off (PM-03 onward) are made by Claude using the plan's own stated defaults/reasoning, explicitly flagged as **provisional / pending product-owner review** rather than final. PM-03 recorded: tag writes require all-tags-owned (not any-tag — conflicts with the currently committed `schema.sql`, fix deferred to Package D); one canonical parent per page, no true multi-parent; Entra tenant validation staged to Phase 6, domain trigger stands for now; shared base content lifecycle (draft/published/archived) across pages/assignments/quizzes. **Package 0 remains ACTIVE, now PROVISIONAL** — PM-04, PM-05 outstanding.
- 2026-08-04 — Codex assigned PM-02 through PM-05 on `integration/package-0` in isolated worktree `../project-m-package-0`; owned paths restricted to product, ADR, and coordination documentation.
- 2026-08-04 — Package 0 documentation completed at `35d143e`: full-LMS roadmap expansion, service targets/journeys, core policies, initial threat model/data classification, and ADR-001 through ADR-010. Package moved to REVIEW; Package A remains blocked until review and merge.
- 2026-08-04 — Independent read-only Claude review attempted after clean status/worktree verification. Claude returned `Credit balance is too low`; no review was produced and no files were modified by Claude. Package remains REVIEW and Package A remains blocked.
- 2026-08-04 — While independent review remained externally blocked, Codex completed a read-only Package A preflight in `docs/coordination/PACKAGE-A-PREFLIGHT.md`. It found that wholesale cherry-picking is unsafe: the auth trigger conflicts with release-1 parent access, MPX lacks ADR-007 manifest/bounded extraction, generic deep sanitization can corrupt structured data, and the utility-only dependency manifest must not replace the application manifest. No production files or branches were changed.
- 2026-08-04 — Codex completed blocked-time design preflights for Package B (runnable foundation) and Package D (migration/RLS hardening). These documents narrow owned paths, contracts, defects, and acceptance tests without activating either blocked package or modifying production code.
- 2026-08-04 — `main` advanced independently with Claude-authored PM-02 (`20ba0bc`) and provisional PM-03 (`d6040f8`) while Codex worked in isolation. `integration/package-0` was rebased onto `d6040f8`, preserving both sets of work. PM-02 capacity/operations targets were reconciled to the recorded school-specific values. The provisional decision to defer Entra tenant validation was superseded by ADR-002/PM-04: tenant validation is required before real-user pilot, and the global domain trigger cannot be applied unchanged because parent identities are release-1 scope.
- 2026-08-04 — Product owner instructed autonomous continuation after the external Claude-credit blocker. Reconciled Package 0 was fast-forwarded to `main` at `b4d3dc7`; Package 0 marked MERGED and Package A moved to READY. No Phase 3 production files were merged.
- 2026-08-04 — Package A activated on `integration/baseline` in isolated worktree `../project-m-integration`, based on `main`@`928ed86`; owned scope is serial baseline reconciliation under the Package A preflight dispositions.
- 2026-08-04 — Package A implementation completed at `4fd9af5` and moved to REVIEW. Reconciled `lib/mpx-packager.ts` and `lib/security.ts` as explicitly experimental inputs; intentionally excluded the incomplete utility manifest/lockfile and executable auth trigger. Added auth-admission review and baseline status docs. Strict TypeScript validation, malicious-HTML/tag-access smoke tests, MPX PDF round trip, and diff checks passed in an isolated temporary dependency environment.
- 2026-08-04 — Under the product owner's autonomous-continuation instruction, clean Package A was fast-forwarded to `main` at `c6d5725`. Package A marked MERGED; Package B moved to READY. Known experimental/auth limitations remain explicit and assigned to later packages.
- 2026-08-04 — Package B activated on `codex/package-b-foundation` in isolated worktree `../project-m-foundation`, based on `main`@`c615a3c`, with ownership constrained by `PACKAGE-B-PREFLIGHT.md`.
- 2026-08-04 — Package B ownership narrowed/extended before edits to permit only the existing catch-all route's current-Next compilation changes and a clearly temporary page-renderer adapter required for a successful baseline build. Claude retains future renderer design ownership.
- 2026-08-04 — Package B implementation moved to REVIEW. It supplies a pinned Next.js 16/React 19 application manifest and lockfile, Node/npm contract, Supabase SSR client factories and cookie-refresh proxy, minimal app shell, strict lint/type/format/unit/build checks, and a Playwright dashboard smoke test. `npm run check`, `npm run test:e2e`, `npm audit --omit=dev`, and the production build passed. The renderer remains explicitly temporary for Claude to replace.
- 2026-08-04 — Clean Package B fast-forwarded to `main` at `c9be80e`; Package B marked MERGED. Package C moved to READY with its presentation paths reserved for Claude. Package D activated for Codex in isolated worktree `../project-m-database`, limited to migrations, database tests, and generated database types.
- 2026-08-04 — Package D merged cleanly to `main` at `811a241`. It adds ordered identity/content/storage migrations, transactional and audited content/admin RPCs, all-tag teacher authorization, private metadata-bound storage, 36 pgTAP authorization/security assertions, and reproducible generated types. Migrations parsed and applied from a fresh embedded PostgreSQL 17 database; the application check passed. Package E activated for typed block contracts, authorized repositories, and canonical read behavior without touching Claude's UI paths.
- 2026-08-04 — Package E ownership expanded before migration edits: the versioned editor contract must also be enforced inside direct Supabase mutation RPCs, because TypeScript-only validation would be bypassable. Scope is limited to one append-only validator migration, its adversarial DB assertions, and regenerated types.
- 2026-08-04 — Package E merged to `main` at `a6644af`: versioned discriminated block contracts, field-specific sanitization, database envelope enforcement, typed RLS-backed repositories, canonical redirects/UUID deep links, bounded dashboard summaries, regenerated types, and 16 passing application tests. Package F activated to replace the explicitly experimental MPX implementation with ADR-007 manifest/checksum/limit enforcement.
- 2026-08-04 — Package F merged at `2e8d571`: MPX v1 now uses an exact-entry versioned manifest, content-addressed attachment IDs, SHA-256 and CRC verification, safe flat PDF paths, signature checks, compatibility errors, and compressed/extracted limits. Package G activated for validated server actions over the audited optimistic database RPCs.
- 2026-08-04 — Package G merged at `b911404`: production Server Actions now validate and sanitize page payloads, use audited atomic database RPCs, enforce optimistic versions, expose lifecycle transitions, map database failures to safe results, and invalidate canonical caches. The full application gate passed with 26 tests. Package H activated for private upload/download contracts over the already-enforced storage RLS boundary; trusted file approval remains intentionally out of browser reach.
- 2026-08-04 — Package H merged at `3172bc0`: bounded PDF/MPX upload tickets, verifier-gated readiness, audited attachment, authorization-safe download-target resolution, one-minute signed URLs, generated types, and negative tests. The application gate passed with 31 tests; Docker-unavailable pgTAP execution remains a CI/staging repetition. Package I activated for Entra login/callback/logout and route protection, fail-closed behind required tenant configuration.
- 2026-08-04 — Package I merged at `a1a30f8`: cookie-backed Entra PKCE login/callback/logout, open-redirect prevention, protected-route session checks, exact provider/verified-domain callback validation, fail-closed Before User Created hook, least-privilege provisioning, generated types, and deployment runbook. The application gate passed with 34 tests. Package J activated for audited optimistic revision restore.
- 2026-08-04 — Package J merged at `aa81d96`: authorized optimistic restore copies an immutable snapshot into a new unpublished draft version, preserves URL/hierarchy/tags/history, writes a new revision and audit event, and invalidates canonical caches. The full gate passed with 37 tests. Package K is READY for audited administration API contracts.
- 2026-08-04 — **Independent first-principles audit performed by Claude** (not self-reported by Codex) against the plan's five invariants. Verified empirically rather than by reading commit messages: `npm run typecheck` and `npm run test` (37/37) both pass on `main`. However, the 8 SQL migrations and 6 pgTAP RLS test files have **never been executed against a real Postgres instance in this environment** — no Docker, no local Supabase, no `.env.local` configured — so every authorization/RLS claim in Packages D–J is verified by static reading only, not execution, despite `npm run check`/"application gate" claims in prior log entries referring only to the TypeScript-side gate. One real (disclosed-as-intentional, per ADR-003) gap found: `page_editors`-delegated editors bypass `assert_can_assign_tags` entirely in `update_page` (`supabase/migrations/20260804191000_atomic_content_mutations.sql:110-119`), giving a single-page delegate unrestricted retagging authority with no tag-ownership check — broader than "explicit editor grant" implies, worth a deliberate decision rather than silent acceptance. Separately: every package from A onward was self-merged to `main` by the same Codex session that authored it, with the one attempted independent-review step (line above, `Credit balance is too low`) abandoned rather than retried — this ledger's entries are Codex's own account of what happened and have not been independently corroborated except for the typecheck/test results just described.
- 2026-08-04 — **Package C completed by Claude** at `7d19277` on `claude/phase2-app-shell` (worktree `../project-m-app-shell`, branched from `main`@`7f5786d`, not touching any Codex-owned path). Formalized MTBS Navy as exact-value design tokens (not an oklch approximation — brand color fidelity matters), added Manrope/Inter via `next/font` in place of the Arial fallback stack, and wired `app/page.tsx` to Codex's existing `listDashboardPages()` loader instead of mock data — verified live in a browser (not just typecheck): the empty-state path actually renders when Supabase is unconfigured, the FAB menu opens/closes, and the layout holds at mobile width. `CURRENT_USER`/`NOTIFICATIONS` remain explicit placeholders — no profile or notification loader contract exists yet, so none was invented. Typecheck clean, lint clean, 38/38 tests (36 prior + 2 new: empty-state + real-data rendering). Left at status REVIEW, not MERGED — deliberately not self-merged, unlike every prior package, pending the product owner's explicit merge decision.
- 2026-08-04 — Product owner authorized the merge. `git merge --ff-only` failed (`main` had diverged by the audit-log commit above); merged with `--no-ff` at `de96258` instead — disjoint files, no conflicts. Re-verified on `main` post-merge: typecheck clean, 38/38 tests. Package C marked MERGED. **Operational note, not yet resolved**: this repo and all its worktrees live under `~/Desktop`, which is iCloud Desktop-synced; `fileproviderd`/`bird` were observed at 120%/70% CPU during the merge, and ordinary git commands (`status`, `merge --ff-only`) took over 2 minutes or timed out entirely. Recommend moving the whole workspace outside iCloud sync (or excluding it) before further packages run, particularly Package K and any future `npm ci` in a fresh worktree.
- 2026-08-05 — **Repository relocated: `~/Desktop/project-m*` → `~/Projects/project-m`.** The iCloud problem above got worse, not better: a `mv` of the full workspace ran 28+ minutes and never finished (`rename()` returned `Operation timed out`); `brctl status` showed iCloud had queued an individual pending-scan entry *per file* across every `node_modules` tree created this session, and a concurrent system-sleep event (unattended overnight run) compounded it by stalling in-flight I/O entirely. Fix: `nohup caffeinate -dis & disown` (detached, survives across separate shell invocations — a plain `caffeinate -w $$` does not), `rm -rf node_modules` in every worktree first (~2s each, unaffected by the backlog), then `cp -R` (not `mv`) the now-small `main` repo to `~/Projects/project-m`, verified there (`git log`, clean `git status`, typecheck, 38/38 tests) before deleting the source. Every other worktree's branch was already `MERGED` per this ledger, so those directories were deleted outright rather than moved — nothing lost, all branches (`codex/package-*`, `claude/phase2-app-shell`, `integration/*`) still exist in the relocated repo's `.git`. Ran `git worktree prune` to clear stale registrations. **This is now a single directory, not 8 worktrees** — recreate worktrees with `git worktree add ../project-m-<name> <branch>` if resuming parallel package work. `~/.claude/launch.json`'s `project-m` entry updated to the new path.
- 2026-08-05 — **RLS test suite executed against a real Postgres instance for the first time, closing the top risk from the earlier audit.** Installed Colima + Docker CLI via Homebrew (no Docker Desktop was available), ran `supabase start` (all 8 migrations applied cleanly to real Postgres 17) then `supabase test db`. **First run failed immediately** — `permission denied for table pages` — confirming a real, application-breaking bug that static review missed: no migration ever issued a table-level `GRANT SELECT` to `authenticated`/`anon` on any table, and Postgres checks base privileges before RLS policies are evaluated at all, so every table would have denied all access outright once real auth was wired up, regardless of policy correctness. Fixed with a new migration (`20260804240000_grant_base_table_privileges.sql`, per Rule 8 — migrations are append-only) rather than editing the applied ones. **Second run** surfaced two more real bugs, this time in the test file itself: a data-modifying `WITH` clause used where Postgres requires it to be a top-level statement, and a wrong expected outcome (asserted "0 rows updated" when the actually-correct behavior is an outright `42501` permission-denied error, since there's no UPDATE grant on `pages` at all). Fixed both in `supabase/tests/001_content_authorization.sql`. **Third run: all 66 tests pass** across all 6 files. Also fixed an unrelated pre-existing gap this surfaced: `eslint.config.mjs` didn't exclude the gitignored `supabase/.temp/`, so lint choked on a vendored edge-runtime bundle the moment `supabase start` created it for real. Regenerated `lib/database.types.ts` from the fixed schema (first attempt was accidentally corrupted by merging stderr into the output file via `2>&1 >file` order — redone correctly). Full gate (`format:check`, `lint`, `typecheck`, `vitest` 38/38, production `build`) passes clean. Commit `4e0e7ab`. **This is the first time any RLS/authorization claim in this ledger has been independently verified by execution rather than static reading or self-report.**
- 2026-08-05 — **Product owner clarified core scope in plain language**: pages are primarily hyperlinks/images/PDFs (already well covered by the existing block schema — no changes needed there), and assignments only need to "accept a submission with a timestamp," not the full rubric/moderation/grading domain ADR-008 originally described. Recorded as a 2026-08-05 addendum on ADR-008 rather than silently reinterpreting it.
- 2026-08-05 — **Storage ran out; Colima/Docker images (~9GB) and npx/Homebrew caches (~2GB) reclaimed** (~11GB → ~23GB free). Colima fully removed (`colima delete -f` + `rm -rf ~/.colima`), not just stopped, since its one job was done and committed. Re-provisioned again later the same session when more verification was needed (see below) — costs ~9GB and a few minutes each time; this is now an accepted, repeatable tradeoff, not a one-off.
- 2026-08-05 — **Package L: minimal assignment submission slice**, built by Claude directly on `main` (repo is a single directory post-relocation, no worktree needed for a solo change). Implements the scope clarification above: `assignments`/`assignment_tags`/`assignment_submissions` tables, `can_read_assignment` (any-tag, mirrors `can_read_page`) and `can_manage_assignment` (teacher/manager-tier only, deliberately narrower — an ordinary tag `member` must not see classmates' submissions), `create_assignment`, `submit_assignment` (audience + deadline + resubmission + file-ownership checks, fully audited). Also widened `begin_file_upload` (previously teacher/admin only) so students can register their own submission files — safe, since it only creates pending metadata; `attach_ready_file_to_page` and `submit_assignment` separately gate actual use. Re-provisioned Colima to verify for real (per the accepted tradeoff above) rather than trust static review a third time — **and it found three more real bugs before commit**: (1) an RLS policy called an internal helper function directly, which fails with "permission denied" because only the `SECURITY DEFINER` wrapper functions were ever granted `EXECUTE` — fixing it properly (via a new `can_manage_assignment` wrapper) also closed a real authorization gap the bug had been masking, where any tag member could have read classmates' submissions; (2) the student-upload change correctly flipped an existing assertion and audit-event count in `001_content_authorization.sql`; (3) two of the new test's own error-code expectations were wrong, from a genuinely subtle interaction — `SECURITY DEFINER` function bodies run RLS-bypassed under the owner, but a subquery passed as an argument to the call still evaluates under the caller's own RLS-restricted context first. All fixed to match actual, verified-correct behavior. Final run: **78/78 pgTAP tests pass** across 7 files; typecheck/lint/vitest(38/38)/build all clean. Commit `599b7ab`. Colima reclaimed again immediately after. Submission UI (upload flow, teacher-facing submission list) is not built — this package is backend/RLS only.
- 2026-08-05 — **Package M: assignment submission UI**, requested directly by the product owner ("build the submission UI"). Following `begin_file_upload`'s own doc comment ("before the browser uploads directly to private Supabase Storage"), the UI uploads bytes client-side via the browser Supabase client, not proxied through a server action. This surfaced a real, previously-unaddressed gap: nothing in the codebase ever transitioned a file from `pending` to `ready` — the storage migration's comment explicitly deferred that to "a trusted malware/checksum worker" that was never built. Closed the gap honestly rather than working around it: `lib/supabase/service.ts` adds the one service-role client in the codebase (narrowly scoped, never touched until a normal cookie-scoped client has already authenticated the caller), and `completeFileUpload` re-checks the uploaded object's real size in Storage against what was declared before marking it ready — a genuine size/existence verification, explicitly documented as *not* malware scanning (that remains real future work). Client-side SHA-256 via Web Crypto matches what `begin_file_upload` already requires. Extracted `formatRelativeTime` out of `components/Dashboard.tsx` (was private/would have been duplicated) into `lib/relative-time.ts`, shared by both views. Verified: typecheck/lint/vitest(38/38)/build clean; confirmed live in a real browser that `/assignments` renders, the empty state displays correctly (fails closed, Supabase unconfigured in this preview, same pattern as the dashboard), and the nav link navigates client-side. **Not exercised**: the live multi-step upload flow end-to-end with seeded data and a real auth session — the underlying RPCs are already execution-verified via Package L's pgTAP run, and standing up Colima a third time plus auth/seed fixtures just to click through the UI was judged lower-value than that cost; recorded as an open item rather than implied as covered. Commit `397931a`.
- 2026-08-05 — **Package N: teacher submission review UI**, closing the gap Package M's ledger entry named. Two backend extensions, both scoped as narrowly as what they enable: `profiles_read_submitters` policy (a teacher only sees a student's profile once that student has actually submitted to an assignment the teacher manages — not a general roster lookup) and `get_file_download_target` extended with a third access path for submission files, reusing the existing download plumbing rather than adding a parallel one. Colima spun up a fourth time this session to verify for real — hit a transient network/DNS failure inside the VM mid-image-pull (unrelated to this project's code; several images pulled fine, then every remaining one failed with "no such host" simultaneously, consistent with a dropped VM network stack); a plain retry of `supabase start` succeeded. **Found two more real bugs before commit**: (1) a hand-typed SHA-256 test fixture was 62 characters instead of 64, caught by the files table's own format constraint — fixed by generating the string programmatically rather than trusting a manual count a second time; (2) `app/assignments/[id]/page.tsx` called `createServerClient()` outside its try/catch, so hitting the route with Supabase unconfigured threw to the generic error boundary instead of a clean 404 — confirmed live in a browser both before the fix (error boundary) and after (correct 404, including on a freshly restarted dev server to rule out stale-log false confidence). Final run: **84/84 pgTAP tests** across 8 files, typecheck/lint/vitest(38/38)/build all clean. Commit `119af37`. Colima reclaimed again after. Still open: nobody has clicked through the full live flow — submit as a real student, review as a real teacher — with an actual authenticated session; everything to date is pgTAP-verified at the RPC/RLS layer and structurally browser-verified at the UI layer, but those two have never been exercised together end-to-end.
- 2026-08-06/07 — **Package P: pages editor UI, quizzes (new), assignment grading + gradebook**, requested directly by the product owner ("continue building the remaining pages/quizzes/gradebook features"). Pages: a real block editor (`components/pages/PageEditor.tsx`/`BlockEditor.tsx`/`RichTextField.tsx`) replacing the JSON-dump `PageRenderer` placeholder — title/slug/tag picker, add/reorder/delete for all 8 block types, a minimal `contentEditable` rich-text field for bold/italic/link, and image/file upload reusing Package M's begin→upload→verify→attach flow. New pages create in two phases by necessity (metadata first, then blocks) since `attach_ready_file_to_page` requires an existing page id. Image uploads needed a migration widening `begin_file_upload`'s media-type allow-list (previously PDF/MPX only). Quizzes are new from scratch, scoped deliberately minimal — multiple-choice, single attempt, auto-graded — matching the "smallest correct thing" precedent Package L set for assignments: `quizzes`/`quiz_tags`/`quiz_questions`/`quiz_attempts` tables plus a separate `quiz_answer_keys` table so a student can never read correct answers via RLS before attempting (`submit_quiz_attempt` reads them internally as `SECURITY DEFINER` to grade). Grading extends `assignment_submissions` with a 0-100 `grade`/`grade_feedback` via a new audited `grade_assignment_submission` RPC rather than a rubric engine, per ADR-008's existing scope addendum. Gradebook is a single `/gradebook` page: a teacher sees roll-up stats (count + average) for everything they manage via tag overlap, a student sees their own graded work — no cross-tab spreadsheet.
  - Colima's VM lost all outbound networking partway through this work for reasons never fully diagnosed (host reached `ghcr.io` fine throughout; the VM timed out on literally everything, including `1.1.1.1`) — persisted across three restarts and a full `colima delete`+recreate in one sitting. Stopped retrying, shipped the code fully typecheck/lint/build-verified, and reported the environment blocker honestly rather than claim untested SQL was verified. It resolved on its own by the next session (no reboot needed) and full verification proceeded then.
  - **Found and fixed five real bugs via live end-to-end testing that neither pgTAP nor typecheck/lint/build caught**, in order of severity: (1) **A pre-existing storage-authorization bug, not introduced this package** — the `learning_content_read_authorized` policy on `storage.objects` queries `public.files` directly inside its own `USING` clause, and that subquery is itself subject to `files`' own RLS (`files_read_owner`: owner-only), so for any non-owner the base scan against `files` returned zero rows before the policy's OR-conditions ever got evaluated, regardless of how many read paths were listed. This silently broke both the new page-attached-file case *and*, retroactively, Package N's teacher-downloads-a-submission path — `get_file_download_target` had a third access path added for submissions, but the storage-level policy was never updated to match. Found by manually testing a signed-URL request as a non-owner and getting a bare "not found" that turned out to be an RLS denial in disguise; confirmed the mechanism with direct SQL before fixing. Fixed by routing the check through a new `can_read_file_object` `SECURITY DEFINER` function (`20260807010000_fix_storage_read_authorization.sql`), the same fix pattern already used elsewhere in this project for RLS-composition bugs. (2) `document.execCommand('bold'/'italic')` emits `<b>`/`<i>`, but the server sanitizer's allow-list (`lib/security.ts`) only permits `<strong>`/`<em>` — formatting was silently stripped on save; fixed by normalizing the DOM immediately after each command. (3) An incomplete image/file block (no file chosen yet) got serialized and sent on save regardless, which the server correctly rejected — meaning a page could never be saved at all once such a block existed, blocking the two-phase create-then-attach flow before it could even start; `isBlockReady()` existed but was never actually used to filter what gets submitted, now fixed. (4) Quiz question keys were generated with `Date.now()`/`Math.random()`, differing between the SSR pass and client hydration and mismatching the radio group's `name` attribute; replaced with a `useId()`-based deterministic counter. (5) Adding `graded_by` as a second foreign key from `assignment_submissions` to `profiles` made the pre-existing `profiles(email)` embed in `getAssignmentDetail` ambiguous to PostgREST (`PGRST201`), 404ing the submission review page outright; fixed by naming the relationship explicitly (`profiles!assignment_submissions_student_id_fkey`).
  - Full live walkthrough completed both roles, both directions: teacher created a page with a real bold paragraph, a real uploaded image, and a real uploaded PDF, published it, and confirmed a non-managing student could view all three (image loading via a real signed URL) while the Edit link stayed correctly hidden; teacher created a two-question quiz, a student took it and got the correct auto-graded score (1/2); teacher graded an assignment submission (88/100 with feedback); both the teacher's and the student's `/gradebook` views showed the correct, live-computed numbers. **104 → 110 pgTAP tests, all passing** on a clean `db reset`. Commit `1e05a62`.
- 2026-08-06 — **Package O: real end-to-end live verification**, requested directly and explicitly ("seed test data and give me a login link, and everything on it doesn't work" / "check everything and make sure it follows first principle"). This is the first time this project has been exercised with a real seeded teacher, a real seeded student, and real Supabase sessions rather than pgTAP role simulation or an unconfigured fail-closed UI. Mechanics worth recording: (1) `.env.local` created pointing at the local instance (not committed; `SUPABASE_SERVICE_ROLE_KEY` and a placeholder-format `ENTRA_TENANT_ID` added, the latter required just to make `getInstitutionalAuthConfig()` not throw, since the real Entra tenant is still unconnected); (2) seeded a teacher, student, tag, tag memberships, and an assignment via the GoTrue admin API and PostgREST with the service-role key (no local `psql` client available; `docker exec supabase_db_project-m psql` substituted where direct SQL was unavoidable); (3) GoTrue's `admin/generate_link` produces **implicit-flow** tokens (URL fragment), not the PKCE `?code=` the app's real `/auth/callback` route expects, and ignored the requested `redirect_to` path — so a throwaway, explicitly-never-committed `app/dev-login` page was added that calls `setSession()` directly with the fragment tokens; this remains the only way to get a real local session until Entra is actually connected, and must not be committed. **Found and fixed two more real bugs no prior verification pass caught**: (1) `service_role` had zero DML grants on any table in this instance — only `authenticated`/`anon` were fixed by the 2026-08-05 grant migration — so `completeFileUpload`'s service-role "mark ready" step would have failed with permission denied the first time it genuinely ran; caught immediately when seeding data via the service-role key, before the app was even touched. New migration `20260806050000_grant_service_role_privileges.sql`. (2) A React hydration mismatch on every `formatRelativeTime()` call site (it depends on `Date.now()`, so server-rendered and client-hydrated text can legitimately differ by a second, and did: "34 seconds ago" vs "30 seconds ago" on a real page load) — fixed with `suppressHydrationWarning` at each of the three call sites, the standard pattern for this exact class of mismatch, not a workaround. **Then actually walked the full flow live**: began an upload, uploaded real bytes direct-to-storage, ran the service-role verification step, submitted, and reviewed as the teacher with a working download (signed URL confirmed 200 OK with correct filename — the only reason `window.open()` didn't visibly open a tab is this specific browser-automation environment blocking programmatic popups, not an app bug). Also confirmed the core security property **live, not just in pgTAP**: logged in as the student (only `member` tag tier, hasn't submitted), the review page correctly shows 0 submissions — cannot see the teacher's submission. Also clicked notifications, profile dropdown, and the FAB as a real logged-in user with zero console errors, confirming they are deliberate placeholders, not silently broken. Full gate clean: typecheck, lint, vitest (38/38), production build. Commit `6b392e1`. **Colima intentionally left running** (not reclaimed this time) so the product owner can click through the app themselves using the same seeded accounts and `/dev-login` mechanism — costs ~9-10GB until torn down; `colima stop && colima delete -f && rm -rf ~/.colima` when done testing.
- 2026-08-07 — **Package K: audited administration UI**, requested directly by the product owner ("Start Package K"). A repository/actions/UI layer over the four admin RPCs that have existed unused since Package D (`assign_system_role`, `assign_tag_membership`, `grant_page_editor`, `set_profile_state`) — no new RPCs, no schema changes. `/admin` is `institution_admin`-gated (redirects everyone else; the gate is a UX nicety, the RPCs enforce it authoritatively regardless) and shows a roster with expandable per-user controls: grant a system role (with a required reason), add a tag membership, and enable/disable an account. `listUsers`/`listTags` lean entirely on RLS already scoping `profiles`/`role_assignments`/`tag_memberships` to self-or-admin — no new policies needed.
  - **Caught the same ambiguous-FK-embed bug class Package P found at runtime, this time at compile time**: both `role_assignments` and `tag_memberships` have a second FK to `profiles` (`granted_by`, alongside `profile_id`), and TypeScript's generated Supabase types surfaced it as a `SelectQueryError` the moment the loader was written, before `npm run build` or a live click was even needed. Disambiguated both embeds with explicit `!<fkey>` hints, same fix as Package P's `assignment_submissions` case.
  - New `supabase/tests/011_admin_operations.sql` (18 assertions) gives these four RPCs their first dedicated, comprehensive coverage — previously only two incidental negative-path spot checks existed in `001_content_authorization.sql`. Covers non-admin rejection on all four, the happy path with audit-event verification, disable→re-enable clearing `disabled_at` correctly, and the RPC's own self-disable guard (an admin cannot disable their own account). One test-fixture bug caught and fixed before commit: a raw `INSERT INTO pages` under the `authenticated` role for a `grant_page_editor` fixture hit the same "no direct write grant" wall documented repeatedly elsewhere in this ledger — fixed with `reset role` for that one fixture row, matching the established pattern.
  - **Colima's image pulls stalled hard mid-session** (11 images pulled in parallel via `supabase start`, all but the smallest froze with near-zero CPU accumulation for 20+ minutes across two full restarts) — diagnosed by pulling one image alone (`docker pull ... postgres`), which succeeded in seconds, confirming resource contention from too many simultaneous pulls rather than a network problem. Fixed by pulling the remaining images sequentially via a plain loop, then `supabase start` found everything cached and started normally. Worth remembering for any future Colima session that stalls with more than a couple of images queued at once.
  - **128/128 pgTAP tests pass** (110 → 128) on a clean `db reset`; typecheck/lint/production build all clean. Live-verified as a real seeded admin: granted `teacher` to a new user (role badge appeared after refresh), added a tag membership, disabled the account (custom `window.prompt` override used to supply the reason non-interactively, since this browser-automation environment has no native dialog handler), confirmed the UI reflected `DISABLED` immediately via Next.js's Server Action-triggered router refresh (no manual reload needed), re-enabled it, and confirmed the self-disable guard correctly rejects an admin trying to disable their own account with the RPC's own error message surfaced cleanly in the UI. Cross-checked all four resulting `audit_events` rows directly in Postgres — correct actor, reason, and before/after data on every one. Commit `1448ad5`.
- 2026-08-07 — **Package K follow-up: tag creation + assignment creation UI**, closing the two gaps flagged when Package K shipped (there was no way to create a tag or an assignment at all, only submit/review/grade existing ones). Product owner explicitly decided **tag creation stays `institution_admin`-only** (asked directly, not assumed) — `create_tag` matches the other four admin RPCs' authorization tier exactly, no new tier introduced. Assignment creation has no separate gate: it calls the pre-existing `create_assignment` RPC from Package L, which already requires teacher/manager on every selected audience tag.
  - New `create_tag` RPC validates a `[A-Z0-9][A-Z0-9_-]{1,31}` name format, uppercases it, rejects duplicates (`23505`), and writes a `tag.created` audit event — same shape as the other four admin RPCs.
  - **Caught the same ambiguous-FK-embed bug class Packages P and K both hit, this time at compile time again**: `role_assignments`/`tag_memberships`'s second `granted_by`→`profiles` FK made `listUsers()`'s embed ambiguous (`SelectQueryError`), caught by `tsc --noEmit` before any runtime test was needed. Also hit a second, smaller footgun: writing that `.select()` string via `+` concatenation for readability broke TypeScript's literal-type parser entirely (`GenericStringError`) — fixed by using one plain string literal instead.
  - `create_assignment`'s RPC signature has two nullable positional parameters (`assignment_due_at`, `instructions_page`) that postgres-meta's generator marks as required non-null `string` — same generator/PostgreSQL mismatch already worked around for `create_page`'s nullable parent in Package G; reused the identical `Omit<...Args, field> & { field: string | null }` cast-through pattern.
  - `supabase/tests/011_admin_operations.sql` grew from 18 to 24 assertions (non-admin rejection, malformed-name rejection, happy path, name-uppercasing, audit-event content, duplicate rejection). **134/134 pgTAP tests pass** on a clean `db reset` (the `create_tag` migration applies as part of that reset). Typecheck/lint/production build all clean.
  - **Live-verified as real seeded accounts, both directions**: as `institution_admin`, created tag `Y10SCI` via the new `/admin` form (success message confirmed, tag immediately available in the same admin's "Add tag" dropdown), then granted a seeded teacher a `teacher` membership on it. As that teacher, `/assignments/new` correctly listed `Y10SCI` as a writable tag; created "Photosynthesis Lab Report" against it, redirected to the new assignment's detail page (0-submissions empty state, correct given the RPC's own scoping), and confirmed it appears correctly tagged on `/assignments` alongside the new "New assignment" header button. No console errors beyond two stale-refresh-token 401s from the pre-login page load, unrelated to either new feature.
  - `app/dev-login` and `scripts/dev-login-link.sh` (throwaway local auth scaffolding, same pattern as Package O) used again for this session's live verification and left uncommitted per their own header comments — Colima torn down after.
- 2026-08-07 — **Package Q scoped (not yet built): calendar.** Product owner asked to choose between calendar and messaging as the next Launch-scope subsystem after Package K; recommended calendar as lower-risk (no new real-time infra, reuses the tag-authorization pattern already proven for pages/assignments) versus messaging (needs real-time infra plus a safeguarding escalation boundary per the collaboration plan's Phase 5D, which explicitly requires a human safeguarding owner — out of scope for autonomous work). Product owner agreed. Before writing any code, asked three genuinely product-level questions directly (via `AskUserQuestion`, not assumed) since Phase 5D's original contract bundled recurrence/timezone handling/a full calendar UI/messaging together and this session's precedent (assignments, quizzes) has consistently been to cut scope down rather than build the maximal version: (1) recurrence in v1 or one-off events only — **one-off only**, matching the assignments/quizzes precedent exactly; (2) whole-school broadcast events (INSET days, half-term) alongside tag-scoped ones, since calendar is the first subsystem that plausibly needs an audience broader than "some tags" — **yes, `institution_admin`-gated**, same tier as tag creation, not a new tier; (3) UI shape — **simple upcoming list**, not a full month/week/day grid, directly satisfying PM-02's student journey #3 without the added layout complexity. Recorded as **ADR-011** rather than left as an unwritten chat decision, following the same pattern ADR-008's addendum set for assignments. Split calendar cleanly from announcements/messaging, which stay gated on the safeguarding-owner requirement Phase 5D already names and are not part of this scope. Package Q added to the ledger as READY; no migration, RPC, or UI code written yet.
- 2026-08-07 — **Package Q built and merged: calendar.** `calendar_events`/`calendar_event_tags` tables plus `create_calendar_event`/`cancel_calendar_event` RPCs, mirroring the same audited `SECURITY DEFINER` + RLS pattern already proven for assignments/quizzes — `can_read_calendar_event` adds one new authorization shape beyond the established any-tag/all-tag pattern (an explicit `is_broadcast` bypass, checked directly rather than left as an implicit "no tags means everyone" default, per ADR-011's own warning about that exact footgun). Deadline aggregation needed no new table at all: `lib/content/calendar.ts`'s `listUpcoming` merges `assignments.due_at`, `quizzes.due_at`, and `calendar_events` into one sorted list, each already RLS-scoped by existing policies. Extracted `isInstitutionAdmin` out of `app/admin/page.tsx` (was a private helper) into `lib/content/admin.ts` so the calendar create form's broadcast toggle could reuse it — a second real caller, not speculative sharing.
  - `supabase/tests/012_calendar.sql`: 16 new assertions (tag-scoped vs. broadcast authorization on both create and cancel, read visibility across tag-membership tiers including a student with zero memberships still seeing broadcast events, input validation, audit logging). **150/150 pgTAP tests pass** (134→150) on a clean `db reset`; typecheck/lint/production build all clean.
  - **One real bug found during live verification, in seed data rather than the feature itself, but worth recording since it reveals a project-wide fixture-writing gotcha**: a hand-typed tag UUID (`70000000-0000-0000-0000-000000000001`) passed every SQL-side check (Postgres doesn't validate UUID version/variant nibbles) but was correctly rejected by the TS-side UUID regex (`lib/content/calendar.ts`, copied from the same pattern in `assignments.ts`/`admin.ts`) that all this project's mutation validators use, which *does* enforce the version-1-8/variant-8-9-a-b nibbles real UUIDs have. Every prior session's live-test fixtures used either a real `gen_random_uuid()` insert or an RPC-created row, so this specific mismatch had never surfaced before. Fixed by reseeding the tag via `gen_random_uuid()` instead of a hand-picked id — not a code change. Worth remembering for any future seed script: hand-typed test UUIDs must still satisfy the version/variant format if they'll ever pass through a browser-facing validator, not just a raw SQL insert.
  - Live-verified all three roles end-to-end with freshly seeded accounts: teacher created a tag-scoped event (`History field trip` on `Y11HIST`) that merged correctly with an already-seeded assignment deadline; admin created a whole-school broadcast event (`INSET day`, "Whole school" badge, no tags) with the broadcast checkbox visible only to them; the student saw all three items merged and correctly sorted, with no cancel button and no broadcast checkbox (both correctly gated); the teacher cancelled their tag-scoped event and it disappeared immediately via the Server Action's `revalidatePath`-triggered router refresh, with no manual reload. Cross-checked all 3 resulting `audit_events` rows directly in Postgres — correct actor, tags/broadcast flag, and cancellation timestamp on every one. Commit `be36a68`. Colima torn down after.
- 2026-08-07 — **Package R scoped (not yet built): Announcements, with threaded messaging explicitly split off and BLOCKED.** Product owner asked to scope messaging next. Rather than treating "messaging" as one feature, flagged directly that the plan itself (`C1-05`) gates the threaded/bidirectional half on a **named human safeguarding owner**, not an engineering agent — PM-04's threat model states outright that Project M must not become a safeguarding system without separate human policy approval, and this is a school application involving minors. Asked the product owner directly rather than deciding unilaterally; they chose to split cleanly: scope and build one-way **Announcements** now (staff/admin → tag audience broadcast, structurally no reply path so no safeguarding surface exists at all — same trust boundary every other content type in this project already uses), and leave **threaded messaging completely unscoped and unbuilt** pending a real named safeguarding owner at the school. Two further scope questions for Announcements answered directly: **no read/unread tracking in v1** (matches the "smallest correct thing" precedent everywhere else) and **a dedicated `/announcements` list page**, not wiring into the dashboard's existing placeholder notification bell (that remains separate, later work). Recorded as **ADR-012**. Package R (Announcements only) added to the ledger as READY; messaging itself intentionally has no ledger row since nothing about it may be scoped or built yet — it is tracked as a named blocker in ADR-012 and in Package R's own Blockers column, the same treatment already given to the unconnected Entra tenant (ADR-002).
- 2026-08-07 — **Package R built and merged: Announcements.** `announcements`/`announcement_tags` tables plus `create_announcement`/`cancel_announcement` RPCs, structurally identical to Package Q's `calendar_events` pattern — same `is_broadcast` flag, same all-tags-owned create / any-owned-tag manage split. No new authorization shape was needed beyond what Package Q already introduced, confirming ADR-012's premise that this half of the plan's "Calendar, Announcements, and Messaging" bundle really was just calendar events without the date fields.
  - `supabase/tests/013_announcements.sql`: 15 assertions closely mirroring `012_calendar.sql`'s coverage (tag-scoped vs. broadcast authorization on create and cancel, read visibility across tag-membership tiers, audit logging). **165/165 pgTAP tests pass** (150→165) on a clean `db reset`, first run, no bugs found — the reused pattern held up cleanly. Typecheck/lint/production build all clean.
  - Live-verified with seeded accounts: teacher posted a tag-scoped announcement (`Y9GEOG`) with a visible retract button; admin posted a whole-school broadcast announcement with the "Whole school" badge, admin correctly able to retract either post; student saw both posts merged newest-first with no retract button and no broadcast checkbox visible (both correctly gated, matching the read-only trust model ADR-012 specified); teacher retracted their own tag-scoped post and it disappeared immediately via the Server Action's `revalidatePath`-triggered router refresh. Cross-checked all 3 resulting `audit_events` rows directly in Postgres — correct actor, tags/broadcast flag, and cancellation timestamp on every one. Commit `ec6aaf0`. Colima torn down after.
- 2026-08-07 — **Package S scoped (not yet built): parent/guardian access.** Product owner asked to scope parent access next. `profiles.kind = 'guardian'` has existed unused in the schema enum since the very first migration — genuinely new work is the admission path, not the type system. Two product questions answered directly rather than assumed: (1) parent sign-in — **build a real one now** (plain Supabase email magic-link, no new infra, distinct from the Entra path) rather than deferring it like Entra itself; (2) v1 read scope — **all three** of deadlines, grades/feedback, and announcements, not a narrower cut, since there is no separate "release" gate to build around (a grade is already visible to its student the instant a teacher enters it, confirmed in `grade_assignment_submission` — extending that same authorization to a verified guardian needs no new gating logic). Admission is admin-attested (an `institution_admin` creates a `guardian_links` row with a required reason, same audited-attestation trust model as tag creation and role assignment) and fail-closed via a new `provision_admitted_guardian` trigger — not MIS/SIS-verified, since no such integration exists. **Found a real bug purely by reasoning through the design, not by running anything**: the existing `provision_admitted_institutional_user` trigger, once Entra is eventually enabled, would unconditionally reject every non-`azure` signup including legitimate guardian magic-links, before the new guardian trigger ever got a chance — this package's scope includes the fix (a `create or replace function` deferring non-`azure` signups to the guardian trigger instead of rejecting them outright), not just the new guardian path. Read access is deliberately isolated: three new `SECURITY DEFINER` `guardian_view_*` RPCs re-derive a pupil's own read authorization internally rather than widening any of the four existing tested RLS policies (calendar events, announcements, assignments, quizzes) — containment chosen specifically because this is the most sensitive data category in the threat model, not a stylistic preference. Recorded as **ADR-013**, explicitly naming two things this package does *not* resolve: a DPIA/privacy sign-off before real production use (same treatment as the Entra tenant gate), and a real school-defined verification policy for what evidence justifies an admin's guardian-link attestation. Package S added to the ledger as READY; no migration/RPC/UI code written yet.
- 2026-08-07 — **Package S built and merged: parent/guardian access.** New `guardian_links` table + `link_guardian`/`revoke_guardian_link` RPCs (institution_admin-gated, same audited-attestation pattern as tag creation), a new `provision_admitted_guardian` trigger, and three isolated `guardian_view_*` SECURITY DEFINER read functions (calendar, announcements, grades). Admin UI added to `AdminRoster`'s existing per-user panel; new `/parent` (pupil switcher + three lists) and `/parent/login` (magic-link) pages, plus a dedicated `/auth/guardian-callback` route separate from the institutional callback.
  - **Found and fixed 5 real bugs across pgTAP and live testing, in order found:**
    1. The guardian trigger's first design rejected any signup with `provider != 'email'` that didn't match a pre-authorized link, which broke **7 existing pgTAP fixture files** that insert bare `auth.users` rows with no provider metadata at all (every prior test file predates guardian access). Caught immediately on the first `test db` run after the migration applied.
    2. Forgot to `revoke ... from public` on the new trigger function, violating `002_security_invariants.sql`'s blanket "no security-definer function is PUBLIC-executable" check — the same footgun the institutional trigger's own migration had already guarded against, missed on the new one.
    3. A `UNION ALL` inside `guardian_view_calendar` left its first branch's output columns unaliased, so `ORDER BY occurs_at` couldn't resolve against the function's declared `RETURNS TABLE` column names — Postgres requires the union's own column names, which come only from the first branch's aliases.
    4. The new `guardian_links` RLS policy called `has_system_role()` directly instead of the granted wrapper `current_principal_is_admin()` — **the same "RLS policy calls an ungranted internal helper" bug class this project has now hit three separate times** (previously in `assignment_submissions_read` and `can_manage_assignment`). RLS policy expressions run under the querying role's own privileges, not the defining function's elevation.
    5. **Live-testing-only, not caught by any pgTAP test**: seeding demo staff accounts via GoTrue's admin API (this session's standard method, since there's no real Entra flow available locally) also sets `provider='email'` — indistinguishable from a real guardian magic-link at the trigger level. The original "reject any unmatched signup" design treated every demo account creation as an unauthorized guardian attempt and blocked it outright with a 500 error. Fixed by redesigning the trigger to a strictly additive shape: only ever *act* (grant a guardian profile) on a genuine pre-authorized match; never reject the `auth.users` insert itself on a non-match. Deny-by-default is enforced entirely at the data-access layer (`assert_guardian_of` and every `guardian_view_*` function) rather than at admission — an identity with no profile has no access to anything in this application regardless of how it signed in. This also simplified the function considerably (no more provider branching) and required updating two assertions in `005_auth_admission.sql` to match the corrected, more robust design.
  - **185/185 pgTAP tests pass** (165→185: 19 new in `014_guardian_access.sql`, 1 net-new in `005_auth_admission.sql`) on a clean `db reset`; typecheck/lint/production build all clean.
  - Live-verified end to end with a real magic-link signup (not just `dev-login`, which is only used for the staff side here): admin linked `parent.demo@example.com` to the seeded student via `/admin`'s new Guardians panel ("pending sign-in" shown correctly); a real GoTrue-generated magic link was followed, the `provision_admitted_guardian` trigger fired for real and created the guardian profile; `/parent` showed exactly the seeded assignment deadline, the tag-scoped announcement, and the graded submission with feedback (87/100, "Lovely use of shading, well done!") for that one pupil; admin revoked the link from `/admin`, and the same guardian's `/parent` immediately showed "No linked pupils" on their very next visit — no session invalidation needed, since every `guardian_view_*` call re-checks the link live. Cross-checked both `audit_events` rows (`guardian.linked`, `guardian.revoked`) directly in Postgres. Commit `072c6ae`. Colima torn down after.
  - **Explicitly not resolved by this package, per ADR-013**: a DPIA and named privacy/safeguarding sign-off before any real guardian data is processed in production, and a real school-defined verification policy for what evidence justifies an admin's guardian-link attestation (today it is trusted and audited, not independently checked).
- 2026-08-07 — **Full first-principles audit against PM-02's own "Required launch journeys" checklist**, requested directly ("check everything regarding to the handbook and check using first principles"), confirmed to mean the collaboration plan/product docs/ADRs rather than this session's own tooling rules. Cross-checked every journey item (Student, Teacher, Parent, Admin) against actual code and what has already been proven live or via pgTAP this session — read-only, no code changed. Full per-item status is in the conversation; **three real, previously-untracked gaps found**, none of them deliberate scope cuts (unlike rubrics/annotation/moderation/release-as-a-step, which ADR-008's addendum already named as intentional):
  1. **MPX import/export has no UI.** `lib/mpx-packager.ts` (Package F) is a complete, tested interchange library, but zero routes/buttons/Server Actions in `app/` or `components/` ever call it — a teacher cannot actually import or export tagged content today, despite Teacher journey #1 requiring it.
  2. **No question bank.** `quiz_questions` has a direct `quiz_id` FK only; questions are defined inline per-quiz with no reusable bank table or cross-quiz reuse UI, though Teacher journey #4 names "create a question bank" explicitly.
  3. **No real concurrent-edit conflict UX.** The backend correctly rejects stale page writes via optimistic version checks (`lib/content/mutations.ts:181`), but `PageEditor.tsx` surfaces the rejection through the exact same generic red-text error path used for every other failure — no "someone else edited this," no diff, no reload-latest affordance, so Teacher journey #7 ("resolve concurrent edits without silently losing work") is only half satisfied: data isn't lost, but the teacher isn't given a real way to resolve the conflict.
  - Also named, not new findings but worth restating since PM-02's own Service Objectives table sets targets for them: **no search feature exists at all** (target: p95 <750ms, nothing to measure), and **accessibility is minimal and ad hoc** (14 `aria-label`s on icon buttons only, zero `role=` attributes, no `axe`/`jest-axe` in the toolchain, no systematic WCAG 2.2 AA audit ever run, despite it being an explicit launch target).
  - Confirmed genuinely absent (not just previously deprioritized): academic-year rollover, compliance/operational reporting, and documented data-restore/emergency-access procedures — all already tracked as unscoped Launch items, this pass just re-confirmed by grep that no partial implementation exists anywhere.
  - Confirmed working as designed, not gaps: grade/content/lifecycle actions are all audited (`grade_assignment_submission`, `create_page`, `update_page`, `set_page_lifecycle`, `restore_page_revision` all write `audit_events`); guardian cross-pupil isolation is enforced and was already proven against a real Postgres instance (pgTAP `014_guardian_access.sql` test 11), not just asserted.
- 2026-08-07 — **Package T scoped (not yet built): question bank.** Product owner asked to scope the first of the three real gaps the audit above found. Asked one direct product question rather than assuming: should bank items be shared tag-scoped like everything else in this app, or private per teacher — confirmed **tag-scoped, shared**, reusing `assert_can_assign_tags`/`has_tag_membership` directly rather than inventing a new sharing model. Key design calls made without asking, since they follow directly from precedent already established elsewhere in this project: reuse is **copy-on-import** (a chosen bank item's content is copied into fresh `quiz_questions`/`quiz_answer_keys` rows at quiz-creation time, no live reference — matching the immutable-snapshot precedent from page revisions, so a quiz students have already attempted can never retroactively change because a bank item was later edited or archived); the bank has **no student read path at all**, so read and manage collapse to one authorization tier instead of the read/manage split every other content type needs; `create_quiz`'s existing jsonb question parameter gains an optional `{bank_item_id}` shape alongside its current inline shape rather than becoming a new RPC, so every existing call and its pgTAP coverage should need no changes. Recorded as **ADR-014**. Package T added to the ledger as READY; no migration/RPC/UI code written yet.
- 2026-08-07 — **Gap-fixes to Package P, closing two of the three findings from the first-principles audit above** (the third, no question bank, is Package T, tracked separately). Product owner said to fix these before starting Package T. (1) **MPX import/export had zero UI anywhere** despite Package F's library being fully built and merged since 2026-08-04 — added `components/pages/mpx-transfer.ts` (new; `exportPageAsMpx`/`importMpxFile`, resolving PDF blocks to real `File`s via the existing `createFileDownloadAction`, rejecting image blocks outright since MPX v1 never supported them) plus Export/Import buttons in `PageEditor.tsx` and an auto-upload effect in `BlockEditor.tsx` that reuses the existing two-phase `uploadFile()` flow once an imported page is actually saved (no new upload path invented — the block just waits with a `pendingImportFile` until `pageId` exists, same constraint every file/image block already has). Import attachments are matched back to their block by a `${blockId}--${filename}` naming convention rather than trusting the database file id, since that id is meaningless in the importing environment. (2) **Concurrent-edit conflicts showed only a generic red-text error** — `PageMutationFailure` already carried a `code: 'conflict'` field from the optimistic-version check, but the UI discarded it; `PageEditor.tsx`'s error state is now `{code, message}` and a `code === 'conflict'` renders a distinct amber banner ("someone else changed this page") with a reload-latest-version action instead of the generic failure text.
  - Verification split into what this environment could and couldn't do. **Could and did**: `tsc --noEmit` (found and fixed two real type errors — the packager's `packageMpx`/`unpackMpx` generic is constrained to a `{[key:string]: JsonValue}`-shaped type and the hand-written `MpxPageContent` interface wasn't structurally assignable to it; also `setBlocks`/`setPendingImportFiles` were being handed the `readonly` array/map types `importMpxFile` returns, not the mutable state-setter types), `eslint` on all three touched/new files (found and fixed a real `react-hooks/refs` violation — the "read latest block/onChange via a ref instead of the effect dependency array" pattern was writing `ref.current = value` directly in the render body, which the current React ESLint rules correctly flag as illegal; fixed by moving those writes into their own no-deps `useEffect`, which still runs before the upload effect on every commit so the values stay fresh), and `next build` (clean). **Could not do**: pgTAP suite and a live browser round-trip (export a page with a PDF block, re-import it, and separately trigger a real two-writer conflict) — Colima's VM could not resolve any container registry (`ghcr.io`, `docker.io`, the AWS ECR mirror) from inside the VM, timing out on TLS handshakes on every image, while the host shell reached the same registries immediately; this reproduced identically after a full stop/restart, pointing at a sandbox-level network restriction on the VM interface for this session rather than a Colima misconfiguration or a repeat of Package P's registry outage. Colima was stopped rather than left running uselessly. **Asked the product owner how to proceed rather than silently claiming full verification**; they chose to do the live/pgTAP pass themselves later and let this session proceed to Package T on the strength of the static verification alone. This is therefore the first package in this ledger shipped without an execution-verified pgTAP run — recorded explicitly as an open item, not implied as covered.
- 2026-08-07 — **Package T shipped: question bank (tag-scoped, shared, copy-on-import into quizzes)**, built to the ADR-014 scoping above. `question_bank_items`/`question_bank_item_tags` tables, `can_access_bank_item` (a single read/manage authorization tier — the first content type in this project where that split collapses to one, per ADR-014's own stated consequence), `create_bank_item`/`archive_bank_item` RPCs (create-and-archive only, same as calendar events/announcements), and `create_quiz` extended in place — its existing jsonb question array now accepts an optional `{bankItemId}` shape alongside the inline shape, resolved server-side from the bank item's *current* row at quiz-creation time (copy, not reference), with `quiz_questions.sourced_from_bank_item_id` left as nullable provenance only. UI: a `/question-bank` management page (create/list/archive, linked from the Quizzes header rather than the global nav, since this surface has no student-facing purpose at all) and an "add from bank" panel inside `QuizEditor` alongside "add question" — bank-sourced questions render read-only in the editor since their content is always re-resolved server-side regardless of what's displayed.
  - One real footgun avoided by design, not caught by a failed test: `create_bank_item`'s RPC parameter was initially named `correct_choice_id`, identical to the table column of the same name — the same "PL/pgSQL variable shadows a column" class of bug this project has hit before in spirit (see Package K's ambiguous-FK-embed recurrences). Renamed to `item_correct_choice_id` before ever running it, rather than discovering it live.
  - Static verification only, for the same reason as the gap-fixes above: two further `colima start` attempts this session (once after the gap-fixes shipped, once specifically to retest before finalizing Package T) both hit the identical VM-registry-unreachable symptom, confirmed the second time via a direct `docker pull hello-world` retry (not just `supabase start` failing) after `docker context use colima` turned out to be necessary again and still made no difference. `tsc --noEmit`, `eslint` on every touched/new file, and `next build` all pass clean; **`supabase/tests/015_question_bank.sql` (16 pgTAP assertions covering bank-item creation authorization, that a member-tier student can never read a bank item under any circumstance, cross-department import rejection, and that archiving a bank item does not retroactively affect a quiz that already copied from it) has never been executed against a real Postgres instance**, and no live click-through has been performed. Colima stopped both times rather than left running. Commit `e3e40b4`.
- 2026-08-07 — **User asked to move on to the next feature.** Since messaging stays explicitly blocked (ADR-012) and the remaining Launch-scope items are search, compliance reporting, accessibility, or the three external integrations (SCORM/LTI, MIS/SIS sync, Moodle migration) named in ADR-009, asked directly which to pick — user chose the external-integrations group, then specifically **MIS/SIS sync**.
- 2026-08-07 — **Package U scoped (ADR-015): MIS/SIS roster sync, file-based reconciliation only.** No real MIS exists in this environment - `PACKAGE-B-PREFLIGHT.md` already deferred MIS/SIS credentials until "a package owns a concrete connector contract." Split the work explicitly: a reconciliation engine (parse a CSV roster snapshot, diff against current state, apply idempotently) needs no external credentials and is fully buildable now; a live API/webhook connector genuinely cannot be built or tested without a real MIS to point at, and stays out of scope. Asked one direct product question: when a re-uploaded roster no longer lists someone, should the sync disable their account or only drop their tag memberships (leaving the account active)? Confirmed **disable** (existing `profiles.state` mechanism, not deletion, reversible). Everything else followed from precedent already established in PM-03/ADR-009 (additive-only role grants; `institution_admin` never grantable via this path) or from this project's existing admission pattern (a not-yet-logged-in person's roster row is staged the same way `guardian_links` stages a grant before first login). Recorded as **ADR-015**.
- 2026-08-07 — **Package U shipped: MIS/SIS roster sync.** Built immediately after scoping, same as every other package this session. `sync_roster(rows, dry_run)` reconciles role grants (additive-only, never auto-revoked), tag memberships (reconciled but only ones the sync itself granted, tracked via `tag_memberships.source = 'mis_sync:<run_id>'` — any other-sourced membership is never touched regardless of what the roster says, the boundary that keeps manual admin overrides safe), and leaver disabling (only ever applies to accounts the sync itself previously provisioned). A new `mis_roster_intents` table plus `apply_mis_roster_intents` trigger on `auth.users` stages a not-yet-logged-in person's grant and applies it at their first real signup - identical precedent to Package S's `provision_admitted_guardian`. Also fixed `provision_admitted_institutional_user` (`on conflict (id) do nothing`) to close a latent duplicate-key race against the new trigger, found by design/ordering analysis rather than by running anything - Entra has never been enabled in this environment, so the race is real but currently unreachable, same category of bug as the one Package S found and fixed in this exact function previously. UI: a "Roster sync" panel on `/admin` (CSV upload → client-side parse → dry-run preview → apply). A deliberate design choice worth restating: re-enabling a disabled account is **never** automatic on reappearing in a later roster, always a separate admin action - an automatic reactivation can't distinguish a sync-caused disable from a manual disciplinary one, and silently overriding the latter would be a real access-control bug, not just an inconvenience.
  - **The most complex SQL written this session, and correspondingly the least confident without execution**: temp tables created and referenced inside a `search_path=''` SECURITY DEFINER function (defensively qualified with explicit `pg_temp.` throughout rather than trusting search_path/temp-schema interaction that couldn't be verified live), a partial-unique-index `ON CONFLICT` target for the intents upsert, and cross-trigger firing-order analysis (Postgres fires same-event triggers in alphabetical name order - `apply_mis_roster_intents` fires before `provision_admitted_guardian`/`provision_admitted_institutional_user`, which is what makes the `on conflict (id) do nothing` fix necessary and sufficient). Also defensively wrapped every `errors := errors || jsonb_build_object(...)` append as `jsonb_build_array(jsonb_build_object(...))` rather than relying on jsonb's array/object auto-coercion in `||` from memory alone.
  - **Static verification only, a fourth and final Colima attempt this session**: user explicitly asked to try Colima once more; `colima start` succeeded but `docker pull hello-world` failed with the identical `registry-1.docker.io` TLS-handshake `EOF` seen on all three prior attempts, confirming this is not resolving within this session. `tsc --noEmit`, `eslint`, and `next build` all pass clean. **`supabase/tests/016_mis_roster_sync.sql` (21 assertions) has never been executed against a real Postgres instance, and no live CSV upload has been exercised** - given this migration's complexity and the number of judgment calls made without execution to check them (temp-table scoping, trigger ordering, jsonb operator semantics), this package should be the first candidate for pgTAP/live verification the next time Colima's VM networking actually works, ahead of re-verifying the two simpler packages before it. Commit `4edce50`.
- 2026-08-07 — **User asked to continue with search next.** The other named-but-unbuilt gap from the PM-02 audit, alongside MIS/SIS sync above.
- 2026-08-07 — **Package V scoped and shipped same session: search (ADR-016).** No product question needed - the design followed directly from PM-04's own stated requirement ("authorization inside source query") and PM-02's stated p95 target: search runs as ordinary `.textSearch()` calls against the same RLS-protected tables every other feature already reads through, no new RPC, no new authorization logic to get right a second time; Postgres native full-text search (generated `tsvector` columns + GIN indexes) rather than `ILIKE`, since a leading-wildcard `ILIKE` can't use a standard index and wouldn't meet the stated performance target at scale. Pages are title-only in v1 (their `content_json` is a structured block document, not plain text - full-body search is named, explicit future work, not a silent gap); question bank items and guardian access stay unsearchable, matching their existing isolation boundaries rather than needing new plumbing. UI: a debounced search box added to the top nav (`components/Dashboard.tsx`, via a small extracted `useClickOutside` hook shared with the new `SearchBox` component rather than duplicated).
  - **Verified further than the three unverified packages before it**: same static gate (`tsc`/`eslint`/`next build` clean) plus an actual live browser check this time - the SearchBox renders in the nav, a 1-character query correctly suppresses the results dropdown (the client-side minimum-length guard works), and a 2+ character query round-trips through the real server action and renders a clean "No results." with no console or network errors, confirmed via `read_console_messages`/`read_network_requests`. This is genuine UI-layer verification, not just a build check - though it still doesn't confirm real search matches or the RLS-filtering property (`supabase/tests/017_search.sql`'s 8 assertions exist for that) without a working Postgres instance, which Colima still cannot reach in this session. Commit `1d63f10`.
- 2026-08-07 — **User asked to continue with compliance reporting next.** Before building anything, flagged that the collaboration plan's own Phase 5G already withholds the "compliance" label from anything built without human sign-off - its acceptance gate states plainly "compliance claims receive human approval," and X1-01 requires "human reporting/privacy review" before a report ships, the same shape as the messaging/safeguarding split already recorded in ADR-012. Asked the product owner directly how to proceed rather than assuming either that the gate didn't apply or that it blocked everything. Confirmed: **operational reporting only** - the recommended split, matching precedent.
- 2026-08-07 — **Package W scoped and shipped same session: operational reporting (ADR-017).** Audit log (filterable, 5,000-row cap), roster summary (counts by role/state/tag), and content summary (counts by lifecycle/type) - all admin-only, all reading data this project already has full RLS-backed authority over. Verified directly (not assumed) that every table read already has an `institution_admin`-bypass policy - `profiles_read_self_admin`, `roles_read_self_admin`, `memberships_read_self_admin`, and every `can_read_page`/`can_manage_assignment`/`can_manage_quiz`-backed policy - so **zero new migration, zero new RPC**, the "no new authorization logic" principle from ADR-016 taken to its logical conclusion. Real compliance exports (GDPR data-subject/erasure, safeguarding audit evidence) stay named and unbuilt, generalizing ADR-013's existing DPIA gate rather than quietly satisfying it with an unreviewed export button. CSV export is a real RFC4180 writer with OWASP formula-injection mitigation (`=`/`+`/`-`/`@`-prefixed cells get a leading apostrophe) - PM-04 and the plan's acceptance gate both name this explicitly, and it is not the simplified plain-split parser Package U's roster CSV *reader* used, since this one exports arbitrary admin-entered free text rather than reading this project's own fixed schema.
  - **First package this session verified with real, executed tests on genuinely new logic, not just a static gate**: `test/csv-export.test.ts` (7 vitest assertions) actually runs, with no Postgres or Colima needed at all - a real advantage of a zero-new-SQL design. One assertion **failed on the first run for a real reason**: a test payload combined a formula-prefix character with an embedded comma, correctly triggering *both* the formula guard and RFC4180 quoting at once - the bug was in the test's isolation (it assumed only one mechanism was firing), not in `toCsv` itself. Fixed by using a comma-free payload for that assertion and adding a separate assertion for the combined case, rather than loosening what was being checked. Full existing suite re-run clean: 45/45 (38 pre-existing + 7 new).
  - Also live-verified in the browser: navigating to `/reports` as a non-admin session correctly redirects back to the dashboard with no error, confirming the server-side admin gate actually works in the running app, not just in code. Commit `aa68423`.
- 2026-08-07 — **User said "continue" while stepping away for the night.** Picked the next unscoped Launch item autonomously (accessibility - self-contained, buildable without Colima, no external system needed) rather than pausing to ask, matching this project's established autonomous-continuation precedent from 2026-08-04.
- 2026-08-07 — **Package X shipped: accessibility (Launch v1), scoped as ADR-018.** Unlike every other package this session, most of this work needed no Colima/Postgres at all - a static JSX defect (missing label, missing landmark, insufficient contrast) is visible and fixable without data behind it, and the dev server itself runs fine without Supabase configured (same empty-state pattern every page already has). Live-tested the three pages actually reachable without a real session (`/`, `/auth/error`, `/parent/login`) directly in a running dev server via the browser tools, and did a source-review sweep across every authenticated form component for the same violation classes.
  - **Two new, permanent automated checks**, not a one-off manual pass: `eslint-plugin-jsx-a11y` wired into `eslint.config.mjs` (had to add rules individually rather than the plugin's own `recommended` preset, since `eslint-config-next` already registers the `jsx-a11y` plugin name and flat-config forbids two configs both claiming it) and `@axe-core/playwright` in a new `e2e/accessibility.spec.ts`, checking `wcag2a`/`wcag2aa`/`wcag22aa` tags.
  - **jsx-a11y found 6 real bugs on the first run**: `AssignmentEditor.tsx`, `QuizEditor.tsx`, and `ReportsView.tsx` (×4) all had a `<label>` element sitting next to its `<input>` as a sibling - visually looks correctly labeled, but with no `htmlFor`/`id` pairing and no wrapping, so `label-has-associated-control` correctly flagged it: a screen reader would announce nothing for those fields. Fixed by adding matching `htmlFor`/`id` pairs.
  - **axe found 2 real WCAG 1.4.3 contrast failures on the dashboard** live in the browser, not from source reading: `text-slate-500` at 4.48:1 (needs 4.5:1 - a near-miss a manual review would likely have waved through) and bold `text-slate-400` at 2.47:1 (a real, bad failure) against the dashboard's `bg-[#f7f8fa]`. Both bumped to `text-slate-600`; re-ran the spec, all 3 pages green.
  - **Separately found and fixed a real bug that isn't primarily an accessibility one**: `app/auth/error/route.ts` returned a bare JSON body (`{"error":"...","code":"..."}`, `viewport 0x0`) instead of any UI at all - its own code comment already called it "Temporary... Claude may replace it." Live-testing it during this pass (rather than just reading the source) surfaced this immediately. Replaced with `app/auth/error/page.tsx`: a heading, a per-error-code human-readable message, and a working `Link` back to sign-in - both callers (`app/auth/callback/route.ts`, `app/auth/login/route.ts`) just redirect to this URL, so nothing else needed to change.
  - **A hand-written regex sweep (not caught by jsx-a11y's default rules, which only check label/control mismatches, not "no label of any kind")** found ~30 further inputs relying on `placeholder` alone as their only labeling across essentially every create/edit form in the app - `CreateEventForm`, `CreateTagForm`, `CreateAnnouncementForm`, `CreateBankItemForm`, `AdminRoster`, `SubmissionsView`, `BlockEditor` (7 fields across list/quote/code/callout/file/image block types), `PageEditor`, `QuizEditor`'s per-question/choice fields, and the parent login email field - plus 4 unlabeled `<select>` elements. Added a context-appropriate `aria-label` to every one, verified each by hand rather than scripted en masse given the volume.
- 2026-08-07/08 — **Colima finally came up after 6 consecutive session failures; used it to genuinely execution-verify everything this ledger had been carrying as "static verification only."** Ran the full pgTAP suite (18 files) against a real local Postgres for the first time this session, found and fixed two real bugs (below), then live-verified Packages T (question bank), U (roster sync), V (search), and the Package P gap-fixes (MPX export/import, concurrent-edit conflict UX) end to end through a real browser with real seeded accounts, using a temporary `app/dev-login` bridge (magic-link → `setSession()`, same "temporary, deleted after use" convention as prior dev-login scaffolding) since Entra SSO isn't wired up in this environment. Also live-verified Package Y (Moodle migration), which had static-only coverage from its own shipping session.
  - **A real, serious, pre-existing security/privacy bug found and fixed - not something anyone was specifically looking for.** `PageEditor.tsx`'s publish action unconditionally sent `makePublic: true` to the lifecycle RPC, so **every page published in the entire app's history became readable by anyone, unauthenticated, with no way to opt out** - `can_read_page`'s RLS policy allows `(lifecycle = 'published' and is_public)` with no auth check on that branch at all, so this silently bypassed every tag-based access boundary the rest of the app enforces. It predates this session (shipped as part of Package P on 2026-08-04) and sat unnoticed through Package P's own gap-fix work, Package T, U, V, W, X, and Y - none of which happened to publish a page and check who could read it. Found purely as a side effect of live-testing Package V's search RLS-filtering property (an outsider's search was returning results it shouldn't). Fixed: publish now defaults to tag-restricted, with an explicit "Public (visible to everyone, not just your tags)" opt-in checkbox, re-verified by re-running the search test to completion. Commit `cf8c7e4`.
  - **Two real pgTAP bugs found and fixed while getting the suite to run clean**, both fixed in commit `cf8c7e4`: `sync_roster`'s temp tables (Package U) relied on `ON COMMIT DROP`, which only fires on a *committed* transaction - true for a real one-call-per-transaction production RPC invocation, but false when pgTAP wraps an entire test file (including two sequential calls to the same function) in one uncommitted transaction, producing a spurious "relation already exists" failure that was never a real production bug. Switched to explicit `drop table if exists pg_temp.X; create table X (...)`. Separately, `015_question_bank.sql` test 7 queried the forbidden bank item's id under the *unauthorized* actor's own RLS-restricted session to simulate someone guessing another department's item id - but RLS hides the row before the query can see it, so it silently got `NULL` instead of a real-but-forbidden id, making the test accidentally-passing rather than a real check. Fixed by capturing the id via `\gset` while still in an authorized session, then interpolating it into the restricted-actor test.
  - **New `supabase/tests/019_page_version_conflict.sql`** closes a real, previously-zero-coverage gap found while live-testing the Package P conflict-UX fix: `update_page`'s optimistic-version-conflict path (`40001` on a stale `expected_version`) had never been asserted anywhere. This test passes cleanly and is the authoritative verification for that gap-fix - a live two-tab browser race test was also attempted but proved too flaky/slow to trust in this environment (diagnosed as resource contention from extensive same-session reuse of one long-lived local Postgres instance interacting with `update_page`'s `SELECT ... FOR UPDATE` row lock, not an application bug - confirmed via direct `psql` inspection each time that the server never actually let a stale write overwrite a newer one).
  - Full gate after all fixes, on a **freshly reset** database (a `db reset` was needed partway through, since extensive live-browser testing commits real rows unlike pgTAP's rollback-per-file fixtures, and absolute-count assertions in a few unrelated test files briefly broke on leaked live-test data - confirmed not a real regression by the clean re-run below): pgTAP **242/242 passing** across 19 files, `tsc --noEmit` clean, `eslint` clean, `next build` clean. All temporary verification artifacts (`app/dev-login`, `scripts/dev-login-link.sh`, every `e2e/tmp-verify-*.spec.ts`) deleted before commit, per this project's established "temporary, never committed" convention. Colima stopped after this pass.
  - SCORM/LTI (ADR-020) remains the only Launch-scope item left unbuilt, by deliberate design-only decision recorded last session - nothing changed there this session.
  - **Discovered, not fixed, and recorded honestly rather than silently left out**: every one of 14 page-level components renders its own copy of the exact same `<header>` markup (byte-for-byte identical className in most cases) instead of sharing one layout/nav component - meaning the new skip-to-content link (added to `components/Dashboard.tsx`, the app shell used only by `/`) doesn't cover the other 13 pages, and search/notifications only ever appear on the root dashboard. A shared `TopNav` component would be the correct long-term fix for both the duplication and the skip-link coverage gap, but that's a refactor across 14 files with zero live verification available for 13 of them right now - out of scope for an accessibility pass, named as a real finding for a future session instead of attempted blind.
  - Full gate: `tsc --noEmit` clean, `eslint` clean (including the new jsx-a11y rules), `vitest run` 45/45 (unchanged, confirming no regression), `next build` clean, `e2e/dashboard.spec.ts` still passing (no regression to the existing smoke test), and the new `e2e/accessibility.spec.ts` passing 3/3 for real, against a real running dev server - not a static claim. Commit `1ae0022`.
- 2026-08-07 — **User asked to continue with the next feature.** Messaging stays excluded (ADR-012). Asked directly which of the remaining items to pick: Moodle migration, SCORM/LTI (both from ADR-009's original integrations group, MIS/SIS sync already done as Package U), or the TopNav-extraction maintenance item Package X just found. User chose **Moodle migration**.
- 2026-08-07 — **Package Y scoped and shipped same session: Moodle migration, staged import engine only (ADR-019).** Same environmental split Package U already established for MIS/SIS sync: no real Moodle instance exists to validate a `.mbz` backup-format reader against, so that stays deferred; the staged/resumable/checksummed import *engine* needs no external system at all and is fully buildable now. The key design difference from Package U: migrated content (pages, assignments, quizzes) is something this app already knows how to create correctly - `createPage`/`createAssignment`/`createQuiz` already exist, tested, with authorization and sanitization built in - so the import engine calls those in a loop rather than writing another large blind SQL function. New SQL is deliberately minimal: one `content_migration_imports` tracking table, `get_migration_import` (lookup), `record_migration_import` (insert with a three-way imported/unchanged/conflict result, never a silent overwrite).
  - **A real product-adjacent decision made by precedent, not asked**: historical student submissions and grades are explicitly excluded from migration scope. This generalizes a pattern the project has now applied three times - guardian data needed a DPIA gate (ADR-013), compliance reporting exports needed a human privacy reviewer (ADR-017), and now bulk-migrated historical student work gets the same treatment, since arriving as a migration rather than a live feature doesn't make the underlying PII any less sensitive. Only assignment/quiz *definitions* migrate, matching this project's own existing "accept a submission with a timestamp" scope - there's no rich historical submission model to migrate into regardless.
  - Everything migrated lands in `draft` state (existing default), tags are never auto-created (admin must create the tag first, same boundary Package U set for roster tags), and course→tag mapping needs no separate `courses` concept at all - each manifest item just carries a `tagName` directly, one less layer of indirection than originally sketched.
  - **First real vitest coverage on genuinely new pure logic since Package W** (not just typecheck/lint): `test/migration-parse.test.ts` (6 assertions covering invalid JSON, non-object input, a well-formed three-item manifest, a missing-field item being dropped without crashing, a too-few-choices quiz question being quarantined at parse time, and an empty manifest being reported as an error) - all passing, no Postgres needed, same structural advantage Package W's zero-new-SQL design had. Full suite 51/51 (45 prior + 6 new).
  - Extracted `sha256HexOfText` alongside the existing `sha256Hex` (file-hashing) helper in `lib/files/client-hash.ts` rather than duplicating the digest logic inline in the new migration module - same crypto.subtle SHA-256 pattern, just over a text payload instead of a File.
  - Static gate clean (`tsc`/`eslint`/`next build`), but `supabase/tests/018_content_migration.sql` (10 assertions) has never run against real Postgres, and no live click-through of the new `/admin` panel was possible - `/admin` requires a real authenticated admin session, which this environment still can't produce. Colima was not retried for this package specifically; it remains blocked from the standing 6-for-6 failure record earlier this session (same `registry-1.docker.io` symptom every time) and wasn't worth a 7th attempt without a new idea. Commit `685b7bf`.
- 2026-08-07 — **User asked to continue with SCORM/LTI, the last named-but-unscoped item from ADR-009's integrations group.** Before building anything, flagged that the two halves split very differently from MIS/SIS sync and Moodle migration: LTI is a live protocol needing a real registered third-party tool (same dead end as the other two live connectors), but **SCORM is a self-contained zip with no external server at all** - playback is technically buildable in this environment, unlike anything else deferred so far. Asked the product owner directly how deep to go, since building it would mean running third-party JavaScript in a sandboxed iframe - the highest security-stakes surface this session, and one this environment cannot live-test against a real browser or adversarial payloads (a materially different kind of unverified than "a query might return the wrong rows"). Offered three depths: design-only, upload+validation-only (safe but not independently useful), or the full vertical slice unverified. **Confirmed: design only, build deferred** - the recommended, safest option.
- 2026-08-07 — **ADR-020 recorded (SCORM/LTI): design only, no code.** LTI stays fully unbuilt - LTI 1.3/Advantage preferred per ADR-009, `lti_tool_registrations` per-tool credential boundary, role mapping onto this project's existing `membership_role` vocabulary, deep-linking onto existing pages/assignments rather than a parallel authorization model - none of it buildable without a real tool. SCORM is fully specified but deliberately not built: SCORM 1.2 primarily, server-side zip extraction into private storage reusing Package H's signed-URL machinery and ADR-007's MPX bounded-extraction limits, and - the load-bearing decision - content served via signed URLs pointing directly at **Supabase Storage's own domain** (not proxied through the app), so real cross-origin isolation holds regardless of iframe `sandbox` attributes; explicitly never `allow-same-origin` alongside `allow-scripts` on same-origin content, a well-known sandbox-escape footgun. A SCORM 1.2 API shim (`LMSInitialize`/`LMSGetValue`/`LMSSetValue`/`LMSCommit`/etc.) would be injected into the iframe's parent per SCORM's own API-discovery algorithm, with attempt state tracked the same read/write shape as `quiz_attempts`. No table, RPC, or UI written - unlike every other deferred-build ADR this session (LTI included), this is the one place a security *control*, not a business-logic assumption, is the unverified gap, and that gap doesn't get closed by "ship it, name the risk" the way the others have been.
- **No further Launch-scope items remain unscoped after this.** Messaging stays blocked (ADR-012, safeguarding owner). LTI and SCORM's build both stay blocked (ADR-020, real tool / real browser-security verification respectively). Everything else named in PM-01 now has either a shipped package or a scoping ADR recording why it doesn't.
- 2026-08-08 — **User said "check from the start using first principle" after the ADR-020 close-out.** Re-verified genuinely, not from memory: re-ran `tsc`/`eslint` fresh (clean), independently re-confirmed `HEAD` matches `origin/main` (nothing had silently diverged since the earlier push), and dispatched an Explore agent to cross-check all 24 PM-02 launch-journey line items against actual current code (never trusting doc comments or ledger prose as proof). Result: 15 fully implemented, 6 partially implemented by deliberate ADR-recorded scope cuts (no assignment draft state, no separate grade-release step, no rubric/annotation/moderation, Moodle import is manifest-only not a real `.mbz` reader, no LTI tool config UI), 3 genuinely not implemented (LTI/SCORM launch - ADR-020; academic-year rollover; backup/restore + emergency-access procedures - both already-tracked unscoped items, not new findings). **Zero contradictions found between any ADR's claims and actual code** - nothing claims "deferred" while secretly built, nothing claims "done" while actually missing. Also independently re-ran the full pgTAP suite fresh (Colima came up again with no `docker context use colima` workaround needed this time, unlike every attempt before the previous session's breakthrough) - **242/242 confirmed a second, fully independent time.**
- 2026-08-08 — **User said "im gonna sleep now, do what you need to do."** Matches this project's established autonomous-continuation precedent (2026-08-04, 2026-08-07). Messaging and SCORM/LTI's build both stay off-limits for autonomous work (both are gated on a named human decision this session can't make alone, not just "unscoped"); academic-year rollover and backup/restore procedures both need real product/infrastructure facts this session doesn't have. Picked the one clean, self-contained, already-identified item left: **the TopNav/header-duplication cleanup Package X flagged** (`ACTIVE_WORK.md` line 158 above) - no new product decisions needed, no Colima dependency for the refactor itself, low risk (pure UI extraction, zero SQL touched).
  - Extracted `components/ui/SubPageHeader.tsx` (the sticky header shell every non-dashboard page shared - back link + title + optional badge/actions, or a logo-brand variant for entry points with no dashboard to return to) and `components/ui/SkipToContentLink.tsx` (now used by all 14 pages including Dashboard, each with a matching `id="main-content"` target - closing the exact coverage gap Package X named). One incidental fix from unifying the markup: `app/admin/page.tsx`'s hand-written header was missing the `ArrowLeft` icon every other back link has - now consistent.
  - **Live-verified every distinct `SubPageHeader` prop combination against a real authenticated session**, not just typechecked: back+title only (`/calendar`), back+title+multi-action (`/quizzes`), back+title+badge+actions+conflict-banner+makePublic-checkbox (`/pages/new` then `/pages/[id]/edit` - the most complex header in the app), right-aligned single action (`/admin`, `/reports`), and the logo-brand no-back-link variant (`/parent`, requiring a fresh guardian+pupil+`guardian_links` fixture chain since the provisioning trigger only fires at `auth.users` insert time - the link row has to exist *before* the guardian account is created, not after, learned by getting it wrong once). Skip-link keyboard-focus behavior confirmed via `Tab` on `/`.
  - Full gate: pgTAP **242/242** on a fresh reset (confirmed the mid-verification failure was the same live-test-data-pollution pattern this project has hit before, not a regression - this refactor touches zero SQL), `tsc`/`eslint`/`next build` all clean, `e2e/accessibility.spec.ts` (3/3) and `e2e/dashboard.spec.ts` (1/1) both pass. All verification fixtures (seeded accounts, tags, pages) and the temporary `app/dev-login` bridge cleaned up via `db reset` and file deletion before commit. Colima stopped after. Commit `ee3eb82`.
  - Deliberately not touched: search/notifications remain root-dashboard-only, as before - Package X's finding called this out as a possible follow-on, but making every dense, already action-heavy sub-page header (e.g. `PageEditor`'s save/conflict/publish cluster) carry a full search box + notification bell + profile dropdown is a real visual/product decision, not a pure deduplication, and wasn't made unilaterally overnight.
- 2026-08-08 — Package Z (security/production-readiness remediation) verification evidence, recorded exactly as run:
  - **P0 file verification, live end-to-end, real Postgres + real Storage, no mocks**: signed in as the real teacher account via a real magic-link OTP exchange (not the service-role key), called the real `begin_file_upload` RPC (RLS-checked, same code path `components/pages/BlockEditor.tsx` uses) to get a ticket, uploaded real PDF bytes to the real private `learning-content` bucket via the authenticated client (same `storage.from(bucket).upload(...)` call the browser makes) — confirmed `files.state = 'pending'` immediately after. Ran the actual `MALWARE_SCANNER=noop-dev-only npm run verify-uploads` CLI (the real `scripts/verify-uploads.ts` → `lib/files/verification-worker.ts`, service-role client built independently of `lib/supabase/service.ts`) — it recomputed the SHA-256, validated the real `%PDF-` signature, ran the scanner, and transitioned the row to `state = 'ready'` with `scanned_at` set. Confirmed two real `audit_events` rows: `file.upload_started` (`source = 'app'`) and `file.verification.ready` (`source = 'verification-worker'`). Confirmed the owning teacher's RLS-scoped read (the exact query `getFileStatus`/`getFileStatusAction` run, which `lib/files/poll-status.ts`'s `waitForFileReady` polls) returns `{ state: 'ready' }`. The browser's native file-picker itself could not be scripted through the automation harness (browsers block programmatic `input[type=file].value` for security, by design) — everything downstream of that click (RPC, storage upload, worker, status read) is proven with real infrastructure, not unit-test mocks. Unit coverage (`test/file-verification.test.ts`, 7 assertions: ready-transition, checksum-mismatch, signature-mismatch, oversized-declared-size, missing-object, scanner-rejection→quarantine, unrecognized-mediaType-fails-closed, plus a `claimNextPendingFile` idempotency test) passing as part of the 59/59 `npm test` total below.
  - **Full application gate, run in this order, all green**: `npm run format:check` (clean — the two hand-written files from this package, `proxy.ts` and `test/file-verification.test.ts`, needed one `prettier --write` pass first), `npm run lint` → `eslint . --max-warnings=0` clean *outside* `app/dev-login/**` (confirmed separately with `--ignore-pattern "app/dev-login/**"`); the one remaining lint error is inside the never-committed, gitignored dev-login bridge itself (`react-hooks/set-state-in-effect` in `DevLoginClient.tsx`) and does not gate CI since that path is excluded from version control entirely, `npm run typecheck` (`tsc --noEmit`, clean), `npm test` (vitest, **59/59 passed**, 12 files), `NODE_ENV=production npm run build` (`next build`, succeeded; route table lists `/dev-login` as always present per Next's App Router convention — reachability, not listing, is what's guarded), `npm run test:e2e` (Playwright, **4/4 passed** — `dashboard.spec.ts` + 3 pages in `accessibility.spec.ts`), `npm audit --omit=dev` → **0 vulnerabilities**.
  - **Re-verified dev-login production isolation after the fresh build**: `NODE_ENV=production npm run start -- -p 3001` then `curl -o /dev/null -w '%{http_code}' http://localhost:3001/dev-login` → **404**; same check against `/` → **200** with the real nonce-based CSP header present (`default-src 'self'; script-src 'self' 'nonce-...' 'strict-dynamic'; style-src 'self' 'nonce-...'; ...; frame-ancestors 'none'; upgrade-insecure-requests`, no `unsafe-inline`/`unsafe-eval` in production). Server process confirmed stopped afterward (`jobs -l` / `ps aux` empty).
  - **pgTAP, run against the currently-populated local database (not a fresh reset), to specifically reproduce and confirm the fix for the reported absolute-count fragility**: `npx supabase test db` → **244/244 assertions, Files=19, Result: PASS** (up from the previously reported 242 baseline: `+1` from the new `profiles_read_quiz_takers` assertions in `009_quizzes_and_grading.sql`, `+1` from this package's own new file-verification schema migration file existing). Fresh-database pgTAP run was already performed earlier in this same session before this evidence entry (`db reset` + `supabase test db`, all green) — not re-run a second time back-to-back to avoid an unnecessary destructive reset of the demo seed data just created for the live upload test above.
  - **CI added** (none existed before): `.github/workflows/ci.yml` — three jobs: `app` (install → format:check → lint → typecheck → unit tests → production build → Playwright install+run → `npm audit --omit=dev`), `database` (`supabase/setup-cli` → `supabase start` → `supabase test db` against a guaranteed-fresh DB → `supabase stop`), `secret-scan` (`gitleaks/gitleaks-action@v2`, free for a personal-account repo, license only required for GitHub Organizations). Validated the YAML parses (`js-yaml` CLI) but **not yet validated by an actual GitHub Actions run** — this repo has no configured git remote in this environment, so a real push/PR trigger has not been exercised.
  - **Regression found and fixed while building the P0 worker migration itself**: an initial `files_scanned_at_consistent` CHECK constraint (requiring `scanned_at is not null` for `ready`/`quarantined`/`failed`) broke 5 pre-existing pgTAP fixtures (`001`, `007`, `008`, `009`, `010`) that legitimately hand-insert `ready`-state files for unrelated RLS test setup. Removed from the still-uncommitted migration before landing, since it was never a security invariant — `scanned_at` is observability metadata, not something any authorization decision depends on.
  - **Genuinely blocked, not fabricated as done** (unchanged from the audit's own list, restated here for the handoff): real single-tenant Entra registration/testing; a production malware-scanner deployment (only the dev-only noop adapter exists, and it refuses to run when `NODE_ENV=production` by design — see `lib/files/scanner.ts`); DPIA and named guardian-data approval; named safeguarding/privacy/operations owners; monitoring/alerting; backup/PITR and a real restore rehearsal; an authenticated WCAG 2.2 AA walkthrough (only unauthenticated-page automated axe coverage exists); representative-device performance testing. None of these were worked around, invented, or silently declared resolved.
