import { ProjectMDesignPreview } from '@/components/design-preview/ProjectMDesignPreview';

export const metadata = {
  title: 'Design preview | Project M',
};

export default function DesignPreviewPage() {
  return <ProjectMDesignPreview />;
}
